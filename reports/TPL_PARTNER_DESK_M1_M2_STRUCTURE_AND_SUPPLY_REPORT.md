# Partner Desk M1/M2 — structure and supply/media delivery

## M2.7B continuation — 2026-09-24 — Mobile approval and native palette pass; customer/export pending

Checkpoint `TPL-PARTNER-M2.7B-20260924-04` advances M2.7B to **`M2_7B_HOTEL_POLICIES_INCLUSIONS_EXCLUSIONS_STAGING_PARTIAL_CUSTOMER_EXPORT_LIVE_PENDING`**. The operator approved the Mobile-origin Hotel policy submission. Canonical state is Approved version 4 with Mobile provenance and exactly 2 governed inclusions plus 2 exclusions. Mobile `eb2689e` unifies the native Hotel Content, Policies, Inventory, Availability, Rates and Media service surfaces on emerald/teal primary, gold accents and white cards while retaining the approved navy Command Center header; Website/Admin colors were not changed. Focused tests pass 3/3, TypeScript/ESLint/diff/Hermes pass, existing APK/app data were reused, and ADB visual observation shows the updated Hotel Content & Amenities card without orange active styling or runtime error. Exact remaining gates are customer approved readback and bounded CSV/XLSX/PDF/Print equality/readability.

The public staging customer-safe API returns HTTP 200 with Approved version 4 and the same four structured selections. Automated desktop/narrow page rendering stopped at Vercel secure login before reaching the application route; no UI PASS/FAIL is inferred. The authenticated operator must open the existing customer staging preview and reconcile CSV/XLSX/PDF/Print to close M2.7B.

## M2.7B continuation — 2026-09-23 — Mobile combined submission pass; review pending

Checkpoint `TPL-PARTNER-M2.7B-20260923-03` advances M2.7B to **`M2_7B_HOTEL_POLICIES_INCLUSIONS_EXCLUSIONS_STAGING_PARTIAL_MOBILE_REVIEW_PENDING`**. Normal Admin review approved the first Website-origin record as version 2. A Mobile edit that still held version 1 was safely rejected with zero mutation, but its error was too generic. Mobile `2278dc2` adds stale-version recovery with preserved input, one clearly labelled combined submission for all policy/inclusion/exclusion sections, and the locked primary emerald/teal plus gold visual hierarchy. Focused Jest, TypeScript, ESLint, Hermes export and diff checks pass; existing APK/app data were reused.

The normal native UI then submitted two inclusions (Room accommodation and Wi-Fi) plus two exclusions (Meals and Transport not included) together. Mobile showed the explicit success message; canonical staging readback is one `pending_review` version-3 record with Mobile provenance and exactly four selections (2/2). No supply, catalogue, real Partner or production data changed. Exact next gate is authorized Admin approval of version 3, then Website/Admin/customer and bounded CSV/XLSX/PDF/Print reconciliation. The fixed 46/213 denominator, `MOBILE_USER_PARITY=COMPLETE`, `PARTNER_MOBILE_PARITY=OPEN` and `PHASE_1_STEP_1=OPEN` remain preserved.

## M2.7B continuation — 2026-09-23 — Website create and Mobile read pass; Admin review pending

Checkpoint `TPL-PARTNER-M2.7B-20260923-02` advances M2.7B to **`M2_7B_HOTEL_POLICIES_INCLUSIONS_EXCLUSIONS_STAGING_PARTIAL_ADMIN_REVIEW_PENDING`**. The operator confirmed the repaired Website form is clean and received “Saved and sent for TPL content review.” Canonical state is one Website-origin pending record at version 1 with check-in 14:00, check-out 11:00, maximum guests 2 and zero optional selections. Native Mobile read the identical Hotel/profile/catalogue/status/version/time/selection summary through the existing APK; no Mobile Save occurred. Exact next gate is normal Admin content review, then the bounded Mobile inclusion/exclusion write and final parity/customer/export closure.

## M2.7B continuation — 2026-09-23 — capacity/status repair staged; Website Save recheck pending

Checkpoint `TPL-PARTNER-M2.7B-20260923-01` starts the Hotel policy/inclusion/exclusion slice using the completed Content Governance engine. `HOTEL_POLICY_PROFILE_V1` and the governed 10 policy / 7 inclusion / 6 exclusion options are implemented across Backend, Website, Mobile, Admin review, approved-only customer projection and bounded exports. Actual PostgreSQL policy coverage passes 7/7; Backend type/build and Website policy tests 2/2, lint and 244-page Webpack build pass.

The first authenticated Website Save exposed a real status mismatch: the retained capacity-8 room is `draft` under the established inventory lifecycle, while the new policy query accepted only `active`. Backend `88402a6` now accepts configurable `draft`/`active` inventory and still excludes `inactive`; it runs as immutable staging release `/home/tpladmin/tpl-api-releases/partner-m2.7b-capacity-88402a6`. Website `501ffd6` fixes overlapping controls, weak borders and undersized form typography in READY Preview `dpl_2MgPsbqtd2wGAyrnM6Fmzt8s2zFo` on `staging.tplgo.com`. Mobile `0e9b720` and the existing APK are unchanged. Production PID `972721` and health remain unchanged.

Current status is **`M2_7B_HOTEL_POLICIES_INCLUSIONS_EXCLUSIONS_STAGING_PARTIAL_WEBSITE_SAVE_RECHECK_PENDING`**. The failed request created zero policy records. Exact next action is one normal authenticated Website Save retry, followed by Mobile/Admin parity, review, Mobile inclusion/exclusion edit, approved customer projection and export reconciliation. Completed M2.6/M2.6A/M2.7A/M2.7A-G statuses, 46/213, `MOBILE_USER_PARITY=COMPLETE`, `PARTNER_MOBILE_PARITY=OPEN` and `PHASE_1_STEP_1=OPEN` remain preserved.

## M2.7A-G continuation — 2026-09-23 — Admin content governance end-to-end pass

Checkpoint `TPL-PARTNER-M2.7A-G-20260923-11` advances the slice to **`M2_7A_G_ADMIN_CONTENT_GOVERNANCE_DYNAMIC_CATALOGUE_STAGING_END_TO_END_PASS`**. The operator confirmed the repaired authenticated PDF is clear, closing the final export gate. The complete Draft → Review → Approve → Publish lifecycle, Website/Mobile catalogue parity, unselected state, customer non-appearance, CSV/XLSX/PDF/Print and canonical version 2 state are now observed. Original 27/15/12 Hotel amenities remain preserved; the separately identified QA publication makes the current catalogue 28/16/12 without changing Hotel selections. Backend runtime `779f701`, Website feature `177929f`, Mobile `e6c18c0`; existing APK retained. M2.7B can begin separately and was not started.

## M2.7A-G continuation — 2026-09-23 — customer exclusion pass; PDF repair staged

Checkpoint `TPL-PARTNER-M2.7A-G-20260923-10` advances governance to **`M2_7A_G_ADMIN_CONTENT_GOVERNANCE_DYNAMIC_CATALOGUE_STAGING_PARTIAL_PDF_REPAIR_LIVE_RECHECK_PENDING`**. The customer staging Hotel page correctly excludes the published but unselected Jacuzzi QA option. CSV, XLSX and bounded Print are operator-confirmed readable. The PDF was overmeshed because each entry was rendered as one pipe-separated line. Backend `779f701` now produces a visually verified three-page landscape table with wrapped cells, repeated headers, page-safe rows and bounded footers. Focused test 1/1, typecheck/build/diff pass; staging release `/home/tpladmin/tpl-api-releases/partner-m2.7ag-pdf-779f701` is healthy on 4100 and production health remains 200. Only live PDF re-download/readability remains pending.

## M2.7A-G continuation — 2026-09-23 — Website/Mobile catalogue parity observed

Checkpoint `TPL-PARTNER-M2.7A-G-20260923-09` advances governance to **`M2_7A_G_ADMIN_CONTENT_GOVERNANCE_DYNAMIC_CATALOGUE_STAGING_PARTIAL_CROSS_CLIENT_PASS_CUSTOMER_EXPORT_PENDING`**. The operator observed the published Jacuzzi QA option on both Partner Website and native Mobile Property amenity selectors, unselected on each. No Save or Partner content mutation occurred. Canonical catalogue remains version 2 with 28 options (16 PROPERTY / 12 ROOM). Customer non-appearance and bounded export/Print checks remain open; no source/deployment/APK change occurred.

## M2.7A-G continuation — 2026-09-23 — governed QA amenity published

Checkpoint `TPL-PARTNER-M2.7A-G-20260923-08` advances governance to **`M2_7A_G_ADMIN_CONTENT_GOVERNANCE_DYNAMIC_CATALOGUE_STAGING_PARTIAL_PUBLISHED_CROSS_CLIENT_LIVE_PENDING`**. The approved Jacuzzi QA entry was explicitly published through the normal Admin UI. Canonical catalogue version is now 2 with 28 published governed entries and 28 active options: 16 PROPERTY / 12 ROOM. The original 27/15/12 baseline remains preserved and separately reported. Publication only exposes an available catalogue option; it does not select the amenity for the Hotel or add it to customer content. Cross-client read and customer non-appearance remain the next observed gates; no code/deployment/APK change occurred.

## M2.7A-G continuation — 2026-09-23 — QA amenity approved; publication remains separate

Checkpoint `TPL-PARTNER-M2.7A-G-20260923-07` advances governance to **`M2_7A_G_ADMIN_CONTENT_GOVERNANCE_DYNAMIC_CATALOGUE_STAGING_PARTIAL_APPROVED_PUBLICATION_PENDING`**. The operator approved the in-review Jacuzzi QA entry through the normal Admin review panel and received the expected “approved, still not published” confirmation. Canonical state is `APPROVED`, entity v1/row v3, published entity none, catalogue version 1 and zero active QA amenity rows. The original 27/15/12 selectable catalogue and existing Hotel selections remain unchanged. No code/deployment/APK change occurred; explicit publication and cross-client verification are next.

## M2.7A-G continuation — 2026-09-23 — QA amenity submitted for review

Checkpoint `TPL-PARTNER-M2.7A-G-20260923-06` advances governance to **`M2_7A_G_ADMIN_CONTENT_GOVERNANCE_DYNAMIC_CATALOGUE_STAGING_PARTIAL_IN_REVIEW_APPROVAL_PENDING`**. The operator used the normal Admin UI to submit `HOTEL_PROPERTY_JACUZZI_QA` for approval review. Canonical readback is `IN_REVIEW`, entity version 1, row version 2, no published entity version, active catalogue version 1 and zero active QA amenity rows. The retained selectable catalogue remains 27/15/12, so Partner/customer surfaces are unchanged. No source, deployment or APK change was required. Exact next action is authorized Admin approval; Publish remains a separate later step.

## M2.7A-G continuation — 2026-09-23 — saved-draft continuation deployed

Checkpoint `TPL-PARTNER-M2.7A-G-20260923-05` adds a filter-independent Saved drafts section on the same governance screen. Clicking **Open draft** now opens the existing detail-first Draft → Review → Approve → Publish flow with Edit draft and Submit for approval actions, rather than jumping directly into the editor. Website `177929f`, focused tests 7/7, lint/diff and 244-route build pass; exact READY Preview `dpl_9kHN7BWMcEv4V5NvPwqK3AxB3L67` is on staging only. The draft lifecycle/data is unchanged pending operator observation.

## M2.7A-G continuation — 2026-09-23 — readable metric typography compacted

Checkpoint `TPL-PARTNER-M2.7A-G-20260923-04` records operator acceptance of the corrected contrast and a remaining oversized-font observation. Website `d27d3eb` keeps the explicit navy/orange/white contrast and reduces metric labels/values to 14px/24px. Tests 6/6, lint/diff and the 244-route Webpack build pass; exact READY Preview `dpl_6hi9JXKX3UGLJuFCwF1EVMcwhwEy` is on staging only. Governance data remains unchanged and Submit for review has not run.

## M2.7A-G continuation — 2026-09-23 — draft exclusion pass; second contrast correction staged

Checkpoint `TPL-PARTNER-M2.7A-G-20260923-03` records **`M2_7A_G_ADMIN_CONTENT_GOVERNANCE_DYNAMIC_CATALOGUE_STAGING_PARTIAL_DRAFT_EXCLUSION_PASS_CONTRAST_RECHECK_PENDING`**. The operator confirmed Jacuzzi is absent in the requested read-only Partner selector check while canonical state remains one private draft, zero active QA amenity rows and published catalogue version 1. No Hotel amenity selection changed.

The first white-card contrast adjustment appeared more faded live. Website `f07dd05` replaces it with explicit opaque navy/orange/white styling and large bold values. Tests 6/6, scoped lint/diff and a 244-route Webpack build pass; exact READY Preview `dpl_C7QqPR2uwHLCWM7dMCzaMUycBusZ` is assigned to staging only. Admin readability recheck is pending; Submit/Approve/Publish have not run.

## M2.7A-G continuation — 2026-09-23 — draft saved; count contrast repaired

Checkpoint `TPL-PARTNER-M2.7A-G-20260923-02` advances governance to **`M2_7A_G_ADMIN_CONTENT_GOVERNANCE_DYNAMIC_CATALOGUE_STAGING_PARTIAL_DRAFT_SAVED_EXCLUSION_LIVE_PENDING`**. The operator saved `HOTEL_PROPERTY_JACUZZI_QA` through the normal staging Admin UI. Canonical readback shows DRAFT entity v1/row v1, no published entity version and zero active QA amenity rows. The selectable catalogue remains version 1 with the retained 27/15/12 counts; governed history is 27 published snapshots plus one private draft.

The faded catalogue-summary typography was a real Website presentation defect. Commit `a160406` provides darker labels, larger bold values and visible white-card borders; focused tests pass 6/6, scoped lint and a 244-route Webpack build pass. Exact Vercel Git build Preview `dpl_8N4T9prSXosiZiYw657HFF5S9nyx` is READY and assigned only to `staging.tplgo.com`. Backend/Mobile code, APK, catalogue publication and existing Partner selections did not change.

Next live gate: refresh Admin and confirm the four summary cards are readable, then verify read-only on Partner Website and native Mobile that Jacuzzi is absent while still private. Submit/review/approve/publish have not run. M2.7A remains PASS; M2.7B has not started.

## M2.7A-G continuation — 2026-09-23 — governance staged; authenticated lifecycle pending

Checkpoint `TPL-PARTNER-M2.7A-G-20260923-01` records **`M2_7A_G_ADMIN_CONTENT_GOVERNANCE_DYNAMIC_CATALOGUE_STAGING_PARTIAL_AUTHENTICATED_LIFECYCLE_PENDING`**. The reusable Content Governance engine, narrow Website & Experience catalogue RBAC, immutable versions, atomic publish/outbox path, safe deactivation and bounded Admin/export UI are implemented, tested and deployed. Website & Experience now separates Pages, Service Catalogue and Content & Attribute Catalogue; future policy/inclusion types remain truthful `Not configured` boundaries.

Migration 0061 adopted the original Hotel V1 catalogue as 27 governed published snapshots without changing the 15 PROPERTY / 12 ROOM split, 14/8 approved selections, two approved content records, three approved media or supply state. Actual PostgreSQL tests passed 6/6; Backend type/build, Website tests 5/5/lint/build/Vercel build, Mobile typecheck and scoped hygiene checks passed. Delivery is Backend `4bd6dc6` on staging 4100, Website `61d722f` in READY deployment `dpl_J47CFFmco8pJwdv7agcyRcn5ifME` on `staging.tplgo.com`, and Mobile `e6c18c0` with the existing APK.

Authenticated Admin lifecycle and cross-client live read remain pending because the supported authenticated browser connection could not initialize after one bounded retry. No QA amenity was created through a shortcut. Exact next action is to save the prepared `HOTEL_PROPERTY_JACUZZI_QA` as **Draft only** in staging Admin, verify it remains absent from Partner/customer projections, then complete normal review/publish and Website/Mobile/export parity. M2.7A remains PASS; M2.7B has not started. The fixed 46 requirements / 213 units and `MOBILE_USER_PARITY=COMPLETE`, `PARTNER_MOBILE_PARITY=OPEN`, `PHASE_1_STEP_1=OPEN` are preserved.

## M2.7A continuation — 2026-09-23 — Hotel content and amenities end-to-end pass

Checkpoint `PARTNER_DESK_M2_7A_HOTEL_CONTENT_AMENITIES_E2E_PASS_20260923` advances the previous Hotel-family status to **`M2_7A_HOTEL_CONTENT_AND_AMENITIES_STAGING_END_TO_END_PASS`** while preserving both completed M2.6 statuses. The common versioned content foundation activates only `HOTEL_CONTENT_PROFILE_V1`; its standardized catalogue contains 27 amenities (15 PROPERTY, 12 ROOM) with stable codes and strict scope. Partner submissions require scoped supply authority, text remains pending until narrow `partner_content.review`, and only approved content enters the customer-safe Hotel projection.

Live staging parity passed. Website submitted the retained synthetic Hotel property description with 14 property amenities; Mobile read the same pending version; Admin showed the exact submission before approval; and the approved version appeared on the staging Hotel page. Mobile submitted the existing room description, structured Queen-bed/City-view values and eight room amenities; Website/Admin read the identical room-scoped record; normal Admin approval produced version 2; and the approved room content appeared only under `Synthetic Deluxe Room — QA Only`. Android Back and background/foreground scope restoration passed. Final canonical evidence is two approved records, 22 selections, four immutable revisions and four events.

The operator confirmed the bounded CSV, XLSX, PDF and corrected content-aware Print outputs are readable and correct. Actual PostgreSQL content coverage passed 7/7 and combined Backend RBAC/export/database coverage passed 14/14, including stale/concurrent writes, rollback and tenant/scope denials. Website focused tests/lint/build, Mobile focused tests/type/lint/Hermes export and Backend type/build passed. Delivery is Backend `021977a` on staging 4100, Website runtime `c0fe85e` in READY deployment `dpl_AV58iLYvRAJe53jPG9ke6AWBBnkh` on `staging.tplgo.com`, and Mobile `9d0b5d5` through the existing APK/Metro. Production and unrelated data remained untouched.

The fixed **46 requirements / 213 units** denominator is unchanged; no whole-program percentage is invented. `MOBILE_USER_PARITY=COMPLETE`, `PARTNER_MOBILE_PARITY=OPEN`, and `PHASE_1_STEP_1=OPEN` remain preserved. The next separate Hotel-only batch is **M2.7B — Hotel Policies and Structured Inclusions/Exclusions**; it was not started.

## M2.6A authenticated live closure — 2026-09-23 — manual-mode foundation ready

Checkpoint `PARTNER_DESK_M2_6A_AUTHENTICATED_LIVE_CLOSURE_20260923_B` advances the prior authenticated-live-pending status to **`M2_6A_MEDIA_MODERATION_AUTOMATION_FOUNDATION_STAGING_READY_MANUAL_MODE`**. In the existing authenticated staging Admin Media Library, the operator observed `Moderation mode: MANUAL` and `No automated provider configured`. Canonical policy readback remains `HOTEL_MEDIA_POLICY_V1` version 1, provider NONE, human review required and both automatic decisions Off; assessment/risk/confidence counts remain zero.

The connected existing Development Client showed all three retained media unchanged: approved property image v3/non-cover, approved property YouTube v5/non-cover and approved room image v3/room cover. Refresh preserved three active rows, Android Back returned to the same scoped Partner Command Center, and background/foreground restored the same synthetic organization and media state. No internal provider/risk/assessment information was exposed on Mobile. Final canonical counts are three approved, zero pending; staging and untouched production API health both returned 200.

No defect, source change, deployment, database/media mutation or new APK was required. Existing Backend/Website/Mobile revisions remain `4016795` / `402851e` / `0fd2f8e`; completed M2.6 remains PASS. The fixed 46 requirements / 213 units and `MOBILE_USER_PARITY=COMPLETE`, `PARTNER_MOBILE_PARITY=OPEN`, `PHASE_1_STEP_1=OPEN` are preserved. The next separate Hotel-only content/amenities/policies/inclusions step was not started.

## M2.6A continuation — 2026-09-23 — common moderation automation foundation staged; authenticated live check pending

Checkpoint `PARTNER_DESK_M2_6A_MEDIA_AUTOMATION_20260923_A` preserves `M2_6_HOTEL_MEDIA_STAGING_END_TO_END_PASS` and adds one provider-neutral, versioned moderation foundation. Only `HOTEL_MEDIA_POLICY_V1` is active. Runtime is explicitly MANUAL/NONE, automatic approval/rejection are Off, and the existing `partner_media.review` human decision remains authoritative. Unknown families fail safely to MANUAL. No AI/provider API, SDK, credential, cost, automatic publication or other family policy was added.

Actual PostgreSQL verification passed 12/12, scoped regression evidence passed, Backend TypeScript/build passed, Website test/lint/Webpack build passed, and the clean Vercel Git build is READY. Backend `4016795` runs only as staging release `partner-m2.6a-manual-4016795`; Website/Admin `402851e` is Vercel deployment `dpl_C9tdnSGbw1jaC5gmJw73GkAs2b5Q` on `staging.tplgo.com`; Mobile stays `0fd2f8e` with no new APK. Protected backup/checksum/readability passed before additive migration 0059. Staging and untouched production API health are 200.

Read-only staging reconciliation shows the Hotel V1 policy as MANUAL/NONE/human-required, zero fabricated assessment rows, and the existing three approved media unchanged. The approved-only projection still contains property image + property YouTube + room image. Authenticated Admin visual verification remains pending because the supported browser connection failed and the new headless session reached Vercel secure login; native readback remains pending because ADB has no connected device. Status is **`M2_6A_MEDIA_MODERATION_AUTOMATION_FOUNDATION_STAGING_PARTIAL_AUTHENTICATED_LIVE_PENDING`**, while completed M2.6 remains PASS.

The fixed 46 requirements / 213 units and `MOBILE_USER_PARITY=COMPLETE`, `PARTNER_MOBILE_PARITY=OPEN`, `PHASE_1_STEP_1=OPEN` are preserved. Exact next action is a strictly read-only operator confirmation of the Admin MANUAL/Hotel-V1/NONE/auto-Off panel and the existing three Approved media in Mobile. The Hotel content/amenities/policies slice has not started.

## M2.6 continuation — 2026-09-22 — M2_6_HOTEL_MEDIA_STAGING_END_TO_END_PASS

Checkpoint `PARTNER_DESK_M2_6_HOTEL_MEDIA_20260922_I` closes the Hotel Media slice. The operator approved the corrected structured YouTube record through staging Admin and confirmed its Approved report/export state. Native Mobile refresh shows the property video as **approved · version 5**, display order 2 and non-cover. The approved-only staging projection returns one property image, the YouTube video and one linked room image; pending/private records remain excluded.

The closure also fixes two usability/security defects: Website/Admin now provide a safe thumbnail-style click-to-load YouTube preview before approval, and Website/Mobile/Backend all prevent videos from becoming gallery covers. The retained invalid video-cover state was corrected through the normal Mobile action, audited as version 4, then reapproved as version 5. Backend `20af18f` is live only on staging 4100; Website `0d4fc8a` is READY in `dpl_DCwyPqnwEbYWbjoQx5gcRjwP5fyn` on `staging.tplgo.com`; Mobile `0fd2f8e` uses the existing APK/app data and Metro.

Focused Website/Mobile checks and builds passed; fresh disposable-PostgreSQL media coverage passed 7/7. Operator live evidence covers Admin approval, Mobile Approved readback, customer gallery display and readable bounded CSV/XLSX/PDF/Print. Final Website, staging API and untouched production API health each returned 200. Production, real Partners and unrelated operational/financial state were not changed.

M2.6 is complete, while the Partner foundation remains OPEN. Fixed 46 requirements / 213 units and `MOBILE_USER_PARITY=COMPLETE`, `PARTNER_MOBILE_PARITY=OPEN`, `PHASE_1_STEP_1=OPEN` remain preserved. The next separate Hotel-only batch is **Hotel content, property/room amenities, policies and structured inclusions**; it was not started.

## M2.6 continuation — 2026-09-22 — M2_6_HOTEL_YOUTUBE_SUBMITTED_ADMIN_REVIEW_PENDING

Checkpoint `PARTNER_DESK_M2_6_HOTEL_MEDIA_20260922_H` records the operator-authorized live YouTube submission. The normal authenticated native Partner Media Library added exactly one structured property-scope YouTube record for the retained staging-only synthetic Hotel. It is display order 2, `pending review`, version 1; raw iframe/HTML was not stored or exposed. The two existing approved images and property/room covers remain unchanged, the existing APK/app data were reused, and production/unrelated Partner data were untouched.

M2.6 now waits only for the normal staging Admin approval of `TPL Hotel Video - QA Only`, followed by Website/App Approved readback, approved-only gallery rendering and bounded export reconciliation. Parser/allowlist/publication-gating and actual-PostgreSQL structured-video coverage remain passing automated evidence. Fixed 46 requirements / 213 units and `MOBILE_USER_PARITY=COMPLETE`, `PARTNER_MOBILE_PARITY=OPEN`, `PHASE_1_STEP_1=OPEN` remain preserved. The next Hotel content/amenities/policies slice has not started.

## M2.6 continuation — 2026-09-22 — M2_6_HOTEL_IMAGE_MODERATION_GALLERY_PASS_YOUTUBE_LIVE_PENDING

Checkpoint `PARTNER_DESK_M2_6_HOTEL_MEDIA_20260922_G` advances the previous image-core status to **`M2_6_HOTEL_IMAGE_MODERATION_GALLERY_PASS_YOUTUBE_LIVE_PENDING`**. Backend `71f1abc` added the audited `partner_media.review` state machine, the two-permission `partner_media_reviewer` role, migration 0058 and an approved/active-only staging Hotel gallery projection. A live defect initially hid the controls because the existing `super_admin` role omitted the new permission; the tested two-line correction is `fb0d244`, now running as staging release `partner-m2.6-final-rbac-fb0d244`. No unrelated Admin permission or new account was added.

Fresh backup/checksum/readability passed before mutation. Canonical staging moved exactly two retained rows from pending to approved through Admin UI, yielding two approval and two moderation-audit events without a duplicate. Property and room covers remain separate. The public staging projection changed from zero pre-approval media to exactly one property image and one room image, while private storage fields remained absent. Operator live checks passed for the customer gallery, Website/Mobile Approved state and readable bounded CSV/XLSX/PDF/Print exports. Actual PostgreSQL approval/rejection/concurrency/idempotency/rollback and tenant-denial coverage passed. Website `4f2f449` / READY deployment `dpl_6NHXJPukEv3jPMgXj3KunPJ5Lqaa` is on staging only; Mobile `9557175` reused the existing APK/app data; production remained healthy and untouched.

The only remaining M2.6 gate is one operator-approved safe YouTube URL through Partner submission, Admin review and approved-gallery readback. Parser tests do not replace that live evidence. Fixed 46 requirements / 213 units and `MOBILE_USER_PARITY=COMPLETE`, `PARTNER_MOBILE_PARITY=OPEN`, `PHASE_1_STEP_1=OPEN` remain preserved. The next Hotel content/amenities/policies slice has not started.

## M2.6 continuation — 2026-09-22 — M2_6_IMAGE_CORE_STAGING_END_TO_END_PASS_MODERATION_YOUTUBE_GALLERY_PENDING

Authenticated live parity for the Hotel media image core is complete. Staging Website rendered both property and room private previews and showed the room as cover/version 2. Read-only Admin Media Library rendered the same two previews, scope and room cover/version without error. No upload or moderation action occurred during these read checks.

Combined with the observed native private preview, single Mobile cover update, Android Back/background restore and canonical readback, the image core now agrees across Backend, Website, Mobile and Admin. Exactly two active pending-review images exist; property and room covers remain independent, and earlier attempts remain removed audit history rather than active duplicates. Current delivery remains Backend `a5f6f0e`, Website `4b3de22`, Mobile `9557175`; no new deployment, APK or production change was needed.

M2.6 remains PARTIAL. Media reviewer authority/Approve-or-Reject workflow, an operator-approved live YouTube link and approved-only customer Hotel gallery display remain OPEN; fresh isolated-PostgreSQL rerun and post-cover live export download were not repeated. Exact next action: authorize the minimum media reviewer policy and audited decision transition, then verify the approved gallery projection. The next Hotel-family content/amenities/policies slice is not started.

## M2.6 continuation — 2026-09-22 — M2_6_IMAGE_CORE_MOBILE_LIVE_PASS_WEBSITE_ADMIN_PREVIEW_PENDING

The existing Development Client reconnected and the operator observed the authorized inline preview for the already-saved room image. No image was uploaded again. The normal Mobile **Set cover** action then succeeded once: the UI retained **Gallery cover updated.**, and the room record changed to cover/version 2. Android Back returned to the same synthetic Partner Command Center; background → foreground restored the selected organization, Media Library, private preview and confirmation.

Canonical read-only staging reconciliation agrees: exactly two active pending-review images remain, with an independent property cover at version 1 and room cover at version 2. The cover change has one committed Mobile update event; earlier attempts remain removed history, not active duplicates. No source/deployment changed: Backend `a5f6f0e`, Website `4b3de22`, Mobile `9557175`, existing APK/app data and the same staging releases remain in use.

M2.6 remains PARTIAL pending authenticated Website and Admin inline-preview observation. Moderation authority, a live operator-approved YouTube link and approved customer-gallery projection remain OPEN. Exact next action: Website Media Library refresh and **Show preview** on the two existing images, confirming the room **Cover · version 2**; no upload or moderation action.

## M2.6 continuation — 2026-09-22 — M2_6_PROPERTY_ROOM_CANONICAL_PASS_PREVIEW_FEEDBACK_DEPLOYED_LIVE_RECHECK_PENDING

The prepared synthetic room PNG was uploaded from native Mobile and the canonical pending room record is visible. Read-only reconciliation confirms two active media rows only: the earlier Website property image and the Mobile room image, correctly separated; two earlier Mobile attempts are `removed` audit history rather than active duplicates. The absent success banner was caused by canonical refresh clearing the notice immediately after success, while saved records had no inline signed preview.

Backend `a5f6f0e`, Website `4b3de22` and Mobile `9557175` now provide permissioned, organization-scoped transient previews and persistent post-refresh feedback. Backend release `partner-m2.6-preview-a5f6f0e` is healthy on staging 4100; production health remains 200. Website deployment `dpl_BH8eNt1wARr2nX6rY6wSxYtYcg6W` is READY on `staging.tplgo.com`; existing APK/app data are retained and Hermes export passed.

Post-deployment native observation is pending because wireless ADB disconnected. No image will be uploaded again. Exact next action: reconnect the existing Development Client, preview the existing room record and set that room image as its gallery cover once, then confirm Website/Admin read-only previews. Moderation, YouTube live link and approved customer-gallery projection remain OPEN. Fixed 46 requirements / 213 units and `MOBILE_USER_PARITY=COMPLETE`, `PARTNER_MOBILE_PARITY=OPEN`, `PHASE_1_STEP_1=OPEN` remain preserved.

## M2.6 continuation — 2026-09-22 — M2_6_PROPERTY_IMAGE_CANONICAL_PASS_REVIEW_UI_CLARIFIED_ROOM_MOBILE_PENDING

The controlled Website retry succeeded. Canonical staging now contains exactly one property-level exterior cover image, version 1, Website provenance, inspected 1200×800 dimensions and `pending_review`; room media remains zero and there is no duplicate. Website `ce842a4` clarifies where the Partner/Admin can see review status, shows review counts, states that Approve/Reject authority is not configured in this bounded slice, and explains the one-image-at-a-time gallery flow with per-image metadata and remaining quota. Focused tests 2/2, scoped lint/diff and a 243-route Webpack production build passed. READY deployment `dpl_DXEYkJqDJC6xch1LAaVio4gezTh9` is assigned to `staging.tplgo.com`.

Pending media remains private. No moderation decision, room upload, YouTube link, customer publication, lifecycle/finance/catalogue change or production mutation occurred. Exact next action: confirm the clarified Website/Admin status presentation read-only, then verify Mobile reads the property image and upload only the prepared synthetic room image. Fixed 46 requirements / 213 units and `MOBILE_USER_PARITY=COMPLETE`, `PARTNER_MOBILE_PARITY=OPEN`, `PHASE_1_STEP_1=OPEN` remain preserved.

## M2.6 continuation — 2026-09-22 — M2_6_HOTEL_MEDIA_CAPABILITY_PUBLISHED_UPLOAD_TRANSPORT_REPAIRED_LIVE_PROPERTY_RETRY_PENDING

Hotel `media` was published through the canonical staging Website & Experience flow and the retained guarded fixture reconciler added only the applicable media permissions to the same synthetic Hotel context. The first property-image attempt created an upload session but the provider's signed-PUT CORS preflight returned 403; confirmation did not run and canonical media rows stayed zero. Bucket-CORS administration failed closed with the existing least-privilege object credential, so no provider policy or broad credential change was made.

Backend `b693e57` adds an authenticated, tenant-scoped raw-image upload using the existing server-issued private reference and confirmation contract; `018de5c` allowlists its protected session header for staging browser preflight. The final immutable staging release is `partner-m2.6-media-018de5c` on 4100. Website `e0bca9d` uses that path and READY deployment `dpl_ADFYHV7xgK7LtBvRaDw5wzvWvEnk` is on `staging.tplgo.com`. Backend focused tests 12/12, actual PostgreSQL media tests 6/6, CORS regression 1/1 and Website regression 1/1 passed with applicable TypeScript/build/lint/diff checks. Live preflight is 204; staging and production health are 200. Mobile remains `0ce38b6` with its existing APK/app data.

No media record, catalogue duplication, application/lifecycle, finance/provider or production mutation occurred. Three old sessions remain pending and unconfirmed; none will be blindly reused or deleted. Exact next action: one controlled fresh Website upload of the repository-owned property QA PNG, then canonical/Mobile/Admin/audit/export reconciliation. Fixed 46 requirements / 213 units and `MOBILE_USER_PARITY=COMPLETE`, `PARTNER_MOBILE_PARITY=OPEN`, `PHASE_1_STEP_1=OPEN` remain preserved.

## M2.6 continuation — 2026-09-21 — M2_6_HOTEL_MEDIA_IMPLEMENTED_STAGING_DEPLOYED_CAPABILITY_PUBLICATION_LIVE_QA_HOLD

The shared property/room Media Engine is implemented, tested and staging-delivered without creating a duplicate media store. Backend `ab3331c` adds guarded private upload sessions, content verification, independent property/room covers, deterministic ordering, pending-review publication safety, structured YouTube IDs, audit and safe exports. Website `b63ee91` is live in READY Preview `dpl_DNj7uPbwtLTbFH6eQNgjMfVPL5Yg` on `staging.tplgo.com`; Mobile `0ce38b6` is JS-only and reuses the installed Development APK/app data. Actual PostgreSQL media concurrency/rollback checks passed 5/5; focused regression, builds, lint/type and Hermes checks passed within the detailed evidence boundaries.

Live media writes have not started. Published staging catalogue version 10 still resolves Hotel without `media`, the retained synthetic fixture has no media grant, and canonical media count remains zero. The remaining gate is the normal Website & Experience Save Draft → Send for Approval → Approve → Publish action for adding only `Media` to the existing Hotel service. After publication, the guarded fixture control can reconcile only `media.read`/`media.write`, followed by the property Website upload, room Mobile upload, Admin/export parity and an operator-approved safe YouTube URL. Production remained untouched and healthy. Fixed 46 requirements / 213 units and `MOBILE_USER_PARITY=COMPLETE`, `PARTNER_MOBILE_PARITY=OPEN`, `PHASE_1_STEP_1=OPEN` are preserved. Detailed evidence: `TPL_PARTNER_DESK_M2_6_HOTEL_MEDIA_LIBRARY_REPORT.md`.

## M2.5 continuation — 2026-09-21 — M2_5_RATES_STAGING_END_TO_END_PASS

Using only the retained staging-only **Stay & Accommodation → Hotel** fixture, Website created one `Synthetic Base Rate — QA Only` for the existing inventory item at INR 1,250.00/version 1 and native Mobile changed only its amount to INR 1,275.00/version 2. Canonical staging readback confirms exactly one rate row, exact minor-unit storage `127500`, and two immutable Website/Mobile events. Website refresh, read-only Admin detail/history and the Supply and media report agreed on item, dates, currency, amount, state and version. Android Back and background/foreground preserved the selected organization.

Backend focused tests passed 15/15 and actual isolated-PostgreSQL checks passed 12/12, covering currency/amount validation, one-winner/stale concurrency, idempotent create, duplicate conflict, atomic audit rollback and permission/cross-tenant denial. Website validation passed 9/9 with scoped lint/type/build; Mobile passed 22/22 with TypeScript, lint, Hermes and public-config checks. The first backend artifact contained stale compiled output and was replaced before live mutation by the exact reviewed build. The first XLSX exposed raw minor units; final backend `ad512f3` now presents `INR 1275.00`, and operator re-download passed. Storage remains integer minor units.

Delivery: Backend commits `c6551a8`, `eba53a3`, final export correction `ad512f31950ee611910cf3bd45a3f4aed71ec342`, staging release `partner-m2.5-rates-ad512f3-r3`; Website `7d837ff9d7834ccc77ffdd1df59414a5c31a495a`, READY Preview `dpl_6vt5iBT2aQVJFtEWXAgezucgxGcP` on staging only; Mobile `5ff91f0124397e5da7e3a991eb7d0faedf074146`, existing APK/app data retained. Production health stayed 200 and no prohibited data changed.

The supported boundary is a versioned canonical base amount with name, date range, account currency and Draft/Active/Inactive state. Tax, commission, markup, discounts, meal/occupancy rules, cancellation/refund rules, dynamic/seasonal/channel pricing, final customer price, booking enforcement, payout and settlement remain outside M2.5. Fixed 46 requirements / 213 units and `MOBILE_USER_PARITY=COMPLETE`, `PARTNER_MOBILE_PARITY=OPEN`, `PHASE_1_STEP_1=OPEN` are preserved. Detailed evidence: `TPL_PARTNER_DESK_M2_5_RATES_AND_RATE_PLANS_REPORT.md`. Exact next action: separately prepare M2.6 with an existing media-eligible service/context; Hotel must not receive Media capability merely for testing.

## M2.4 continuation — 2026-09-21 — M2_4_AVAILABILITY_STAGING_END_TO_END_PASS

Using only the retained staging-only **Stay & Accommodation → Hotel** fixture and its existing `Synthetic Deluxe Room — QA Only` inventory item, Website created one availability range for 2026-10-01 through 2026-10-03 at 6 of 8/version 1. Native Mobile read the same row and changed only available capacity to 5 of 8/version 2. Canonical backend readback, Website refresh and Admin read-only detail then agreed on the dates, value and version; two immutable availability events exist and the latest safe client-surface label is `Mobile Partner Desk`. Android Back, background/foreground and refresh preserved the scoped synthetic organization.

Focused availability/export tests and actual isolated PostgreSQL concurrency/rollback coverage passed: one winner/one stale rejection, overlap/date/capacity validation, atomic audit rollback and permission/cross-tenant denial. CSV/XLSX/PDF matched the canonical record. The initial Print action printed the full Admin page; Website commit `d3cefac12a29f70d8a701fee539af310a921f004` now prints only the bounded Supply and media report with availability/history, and operator live confirmation passed. The earlier missing Admin Mobile-origin history was fixed by `7151d46`; the backend scoped-permission navigation correction is `8e20766` and runs in staging release `partner-m2.4-navigation-8e20766-r2`. Final Website Preview `dpl_G7FZzf4WSMYK6Cq2wZACgdtV8CQu` is READY on staging; Mobile `13b2282` reused the existing APK/app data.

No second availability row, rate, media, booking, finance, catalogue, application, activation, payout or production mutation occurred. Fixed 46 requirements / 213 units and `MOBILE_USER_PARITY=COMPLETE`, `PARTNER_MOBILE_PARITY=OPEN`, `PHASE_1_STEP_1=OPEN` are preserved. Detailed evidence: `TPL_PARTNER_DESK_M2_4_AVAILABILITY_REPORT.md`. Exact next action: start M2.5 Rates and supported rate-plan rules as a separate batch; it is not started here.

## M2.3 continuation — 2026-09-21 — M2_3_INVENTORY_AND_CAPACITY_STAGING_END_TO_END_PASS

The staging-only **Stay & Accommodation → Hotel** fixture now has exactly one canonical inventory item. Website created `Synthetic Deluxe Room — QA Only` with capacity 6/version 1; native Mobile read it and changed capacity to 8/version 2; Website and read-only Admin then showed the same capacity/version and immutable history. CSV/XLSX/PDF/Print live output matched the record and excluded private fields. Availability, Rates and Media remain zero.

The initial Website `Failed to fetch` was a CORS preflight defect: `X-TPL-Client-Surface` was sent by the Website but absent from the backend allowlist, so no POST or row existed. Backend `87fad64` fixes only that contract and is live on staging 4100 release `partner-m2.3-cors-87fad64`; production 4000 remained healthy/untouched. Website `04a2bcb` is live in READY Preview `dpl_BhA5iYGS3Z9CJEBsPGCxPgk5giZC` on staging only, with non-overlapping white/orange inventory fields and a right-side Download & print box. Mobile `fcafa96` reused the installed APK/app data.

Focused Backend/PostgreSQL concurrency, stale-write, idempotency, rollback, authorization and export checks passed; CORS route regression passed 43/43. Website validation 6/6, scoped lint and Webpack builds passed; Mobile validation 6/6, TypeScript, lint and Hermes/public-config checks passed. Android Back and foreground restore retained the synthetic organization. Detailed evidence: `TPL_PARTNER_DESK_M2_3_INVENTORY_AND_CAPACITY_REPORT.md`. Fixed 46 requirements / 213 units and Mobile User COMPLETE, Partner Mobile OPEN, Phase 1 Step 1 OPEN are preserved. Exact next action: M2.4 Availability may start separately; it is not started here.

## M2.2A continuation — 2026-09-21 — M2_2A_SUPPORTED_SYNTHETIC_SERVICE_CONTEXT_STAGING_READY

The prior M2.2 fixture HOLD below is historical. The published staging catalogue was freshly counted: 15 domain roots, 13 operational domains, 239 active/selectable services, 246 entries and 7 inactive/non-selectable entries. Existing **Stay & Accommodation → Hotel** has supply/capacity/availability/rates but no media capability. One guarded, audited, idempotent staging-only synthetic Hotel context was created with an existing QA owner's scoped `supply.read`/`supply.write` membership; no media or payout permission, application, inventory, rate, availability or media records. The submitted application, unrelated active organization, catalogue and production remain unchanged.

Backend commits `3348732` and `7907450` were pushed; only staging Backend 4100 switched to release `partner-m2.2a-fixture-20260921` after a readable protected backup. The release also reconciles the deployed catalogue-coverage route with the tested `partner_service_catalogue.read` guard. Focused tests 12/12 for M2.2 and 51 passing for M2.2A, TypeScript/build/CLI syntax/diff checks passed; 13 opt-in isolated-PostgreSQL tests could not run locally, and true DB concurrency/injected rollback remain uncertified. Staging creation and idempotent repeat passed; production and staging health were 200. Website and Mobile source/APK/app data were unchanged.

Operator-observed read-only staging Website showed the new synthetic organization, Hotel and empty Inventory & Rates. Operator-observed Admin All Partners detail showed the same fixture and synthetic/staging label without error. Connected native Mobile then showed the same fixture/Hotel, the distinct Integration status and Inventory & Rates destinations, an empty supply form, and correct Android Back; no Save/Upload occurred. Canonical read models show one active Hotel service, `canWriteSupply=true`, `canWriteMedia=false`, and zero rows. Website/Mobile label commits `0dc34e5`/`69fe9d9` clarify Integration status without changing Inventory & Rates; clean Website Preview `dpl_2XMVBHEBbB35uuyb5MPsgzMkNDoc` is READY and assigned to staging, while Mobile reused the installed APK/Metro. Backend `e76d03a` excludes the synthetic fixture from Summary aggregates and is live in staging release `partner-m2.2a-summary-e76d03a`. M2.3 supply writes have not started; M2.6 media needs a separate eligible service decision/context. The detailed evidence is in `TPL_PARTNER_DESK_M2_2_CAPABILITY_COVERAGE_AND_SAFE_FIXTURE_REPORT.md`. Fixed 46 requirements / 213 units and Mobile User COMPLETE, Partner Mobile OPEN, Phase 1 Step 1 OPEN are preserved.

## M2.2 continuation — 2026-09-21 — M2_2_CAPABILITY_COVERAGE_IMPLEMENTED_FIXTURE_HOLD (historical)

The bounded M2.2 capability projection is implemented in the backend and covered by focused tests. The static canonical source contains 242 active published service items across 13 configured domains with unique stable codes; the latest staging publication evidence records 15 published domain roots because Currency Exchange and eSIM Provider were added as inactive, non-selectable roots with no selectable services. UF0 separately recorded 246 published catalogue entries including non-selectable grouping/root and archived QA entries; this is not a sellable-service count. Runtime staging item counts are kept separate until an authenticated published-catalogue refresh is available. The projection groups services by capability metadata into reusable families, marks 221 mapped and 21 Medical/Care rows intentionally unsupported for this supply milestone, and finds zero unmapped static rows. Unknown capabilities fail closed. The authenticated Admin read endpoint is deployed to Backend staging release `/home/tpladmin/tpl-api-releases/partner-m2.2-capability-20260921` and requires `partner_service_catalogue.read`; local/public health returned HTTP 200 and the anonymous endpoint correctly returned HTTP 401.

No eligible isolated staging synthetic active-service context was found. The submitted application and the separate pre-existing active organization remain excluded from mutation; existing fixture helpers are isolated-test utilities, not a supported staging activation control. No fixture, catalogue, organization, permission, inventory, rate, availability, media, activation, payout or notification was changed. M2.3 is blocked on a supported staging-only synthetic activation with scoped supply/media permissions and retention ownership. The detailed evidence is in `TPL_PARTNER_DESK_M2_2_CAPABILITY_COVERAGE_AND_SAFE_FIXTURE_REPORT.md`. Focused backend coverage/catalogue tests passed 12/12, TypeScript and build passed, and the changed backend diff passed `git diff --check`. `MOBILE_USER_PARITY=COMPLETE`, `PARTNER_MOBILE_PARITY=OPEN`, `PHASE_1_STEP_1=OPEN`; the fixed 46-requirement/213-unit denominator is unchanged.

## M2.1 closure continuation — 2026-09-21 — MOBILE_LOAD_AND_DESK_USABILITY_STAGING_LIVE_PASS

**Previous status:** `M2_1_MOBILE_LOAD_AND_DESK_USABILITY_STAGING_PARTIAL_PARTNER_HOME_LIVE_PENDING`  
**New status:** `M2_1_MOBILE_LOAD_AND_DESK_USABILITY_STAGING_LIVE_PASS`

The existing wireless ADB device was connected, the earlier phone-call overlay was gone, and the existing Development Client/Metro session was reused. The operator's two profiles were visible; the read-only check selected **the existing ACTIVE Partner profile**, not the submitted application. Partner Home loaded without a hang or crash and retained the selected staging-only organization context and location. The header, account health, selected-service count, active-service count, inventory/media counts and Action Required area rendered. The screen correctly states that no Partner action is required while three service setups await TPL review; no task was misrepresented as a Partner action.

### Menu-by-menu live result

| Destination | Observed result |
|---|---|
| Command Center | Opened as the active Partner Home; current organization and command-center state rendered. |
| My Business | Present in More with `Ready and authorized`. |
| Services | Opened to My Domains & Services; selected services and setup state rendered. |
| Inventory & Rates | Opened; truthful read-only/setup state: supply not ready for an active service, availability requires inventory, base-rate rules are separate. |
| Media Library | Opened; truthful read-only state: `0 of 100 active images`, media read-only/not applicable to an active service. |
| Bookings / Orders / Leads / Cases | All named destinations present in More; Bookings showed setup incomplete and Orders/Leads/Cases showed not applicable where appropriate. |
| CRM | Present with not-applicable reason for the selected services. |
| Finance | Opened separately and showed `planned but not available yet`; no financial value was shown as zero. |
| Settlements | Opened separately and showed `planned but not available yet`. |
| Reports | Opened the read-only Supply and media report plus Activity & Reports export links; no write action was used. |
| Integrations | Present with `TPL approval is still pending`. |
| Compliance / Support | Present with `Ready and authorized`. |
| Team & Access / Settings | Present with `planned but not available yet`. |

The More sheet was touch-scrollable and showed the full approved menu set in order. Explanations used customer-facing reasons (`Ready and authorized`, `Complete the required setup`, `not applicable`, `planned but not available yet`, and `TPL approval is still pending`); implementation-unavailable states were not relabelled as permission failures. Finance and Settlements remained distinct. No protected data or raw internal error was visible.

Native Back from Inventory & capacity returned to the same scoped Partner Command Center. A background → foreground cycle returned to the same active organization and Home state. Pull-to-refresh completed normally and preserved the same organization, counts and action state. No inventory, availability, rate, media, catalogue, activation, approval, permission, payout or application data was mutated.

### Evidence and runtime boundary

New live evidence is the ADB-observed active-profile navigation above. Previous automated evidence remains separate: focused Partner/session/application API tests **26/26 PASS**, Mobile TypeScript **PASS**; the existing repository ESLint debt remains documented and was not changed. No new source code, native dependency, APK, deployment or backend/Website release was required for this closure. Current runtime is Metro `http://10.236.148.65:8081`, device `adb-6d2a1e79-9Qx0mJ._adb-tls-connect._tcp`, existing APK `com.tplgo.mobile` v1.0.0, Mobile source HEAD `0d773f50a73a55dd44f5f8b5a21e14d584e0aa90` with preserved working-tree overlay. Website staging remains `eb4b59888830a472f848251c51439fa955296fc7` / Preview `dpl_4fGfxwvHqwQi2RKS2vHV8qFL7dQ9`; Backend staging remains `5a1e286958c9111f6c2310f45fa30af1403e1771` / release `partner-m1m2-rbac-3b5f82e`, health 200. Production and all application/organization/catalogue data remain untouched.

M2.1 is closed only for load and read-only desk usability. Supply/media bidirectional writes, eligible synthetic service-context certification, broader Partner operations and `PARTNER_MOBILE_PARITY` remain open. The fixed 46-requirement / 213-unit denominator is unchanged and no completion percentage is invented. `MOBILE_USER_PARITY=COMPLETE`, `PARTNER_MOBILE_PARITY=OPEN`, `PHASE_1_STEP_1=OPEN`. **Exact M2.2 prerequisite:** prepare one isolated, authorized synthetic active service context through supported staging controls, then certify the existing Website/Mobile supply and media writes, Admin readback and export equality. M2.2 is not started automatically here.

## M2.1 continuation — 2026-09-21 — MOBILE_LOAD_RECOVERY_PARTIAL

**Current status: `M2_1_MOBILE_LOAD_AND_DESK_USABILITY_STAGING_PARTIAL_PARTNER_HOME_LIVE_PENDING`.**

The native Development Client was diagnosed without reinstalling the APK, clearing app data, changing the API target or changing source. The first Metro start failed before any bundle request because the existing `.expo/dev/logs/start.log` could not be opened (`EPERM`). This is a local Metro filesystem/cache permission/lock condition, not an APK, bundle, API or JavaScript runtime failure. A single bounded retry using the existing Metro command with the required filesystem access succeeded. Metro served `http://10.236.148.65:8081`; Android completed the bundle (`expo-router/entry.js`, 1,912 modules), the Development Client connected, and the Partner profile chooser rendered. The device reached the app-development health endpoint with HTTP 200. No production target, authentication data, application, organization or catalogue record was changed.

The focused Mobile checks passed: Partner/session routing and application API tests **26/26**, TypeScript **PASS**. ESLint was retried with filesystem access and remains **FAIL on pre-existing repository lint debt** in `src/components/partner/partner-home.rendered.test.js` (40 errors) plus six existing style warnings in `partnerSupply.ts`; no M2.1 source change caused these findings. The installed APK remains `com.tplgo.mobile` version `1.0.0`, existing app data is preserved, and no new APK was created. Metro currently serves the preserved working-tree overlay, so the runtime is not represented as a clean commit-only build.

Live boundary: the device was returned to the existing submitted application and showed the canonical read-only “Application submitted / Submitted for review” status, including the no-activation/no-payout message and verified contact summary. The active-organization Partner Home and current More-menu traversal were not re-certified in this continuation because the first profile tap selected the submitted profile and a transient phone-call overlay interrupted the active-profile check. Earlier valid M1 evidence remains: native More exposed the approved named menu set with contextual reasons; Website Partner Home showed Inventory & Rates and Media Library; Admin read-only Partner Desk section map and Supply & Media status were confirmed after the prior staging remediation. Those observations do not upgrade M2.1 to a full live pass.

Current deployed references remain unchanged: Website staging source `eb4b59888830a472f848251c51439fa955296fc7`, Preview `dpl_4fGfxwvHqwQi2RKS2vHV8qFL7dQ9` (alias `staging.tplgo.com`); Backend staging source `5a1e286958c9111f6c2310f45fa30af1403e1771`, release `/home/tpladmin/tpl-api-releases/partner-m1m2-rbac-3b5f82e`, health 200; Mobile source HEAD `0d773f50a73a55dd44f5f8b5a21e14d584e0aa90` with preserved dirty overlay, Metro `10.236.148.65:8081`, APK build retained. Production remains untouched.

The fixed **46 requirements / 213 units** denominator is unchanged; no completion percentage is invented. `MOBILE_USER_PARITY=COMPLETE`, `PARTNER_MOBILE_PARITY=OPEN`, and `PHASE_1_STEP_1=OPEN` remain preserved. Exact next action: on the connected device, return to the TPL app after the call overlay is gone, select the existing active Partner profile (read-only), verify Partner Home and More destinations including native Back/background restore, and record the result. If the active profile still hangs after the successful bundle/API checks, capture its bounded sanitized request/error and make only that narrow repair. Then proceed to **M2.2 Safe Synthetic Service Context**; do not start it automatically in this batch.

## Current checkpoint — 2026-09-20

**PARTNER_DESK_M1_M2_IMPLEMENTED_STAGING_DELIVERED_ADMIN_DETAIL_REMEDIATION_LIVE_QA_PASS.** Scoped Backend and Website corrections now expose the canonical Partner Desk section map and read-only supply/media status inside the existing Admin Partner detail hierarchy. The operator confirmed the repaired Admin screen after staging refresh. Authenticated staging supply/media writes and bidirectional Website–Mobile–Admin equality remain OPEN because no eligible isolated synthetic active service was mutated. This is not full Partner foundation certification.

## Admin detail remediation — 2026-09-20

The operator identified a real Admin defect: All Partners detail showed the established five Admin tabs but hid the Partner Desk section map and did not show supply/media status. The narrow repair preserves the locked Admin structure (`Overview / Applications / All Partners / Reports` and Partner detail tabs) and adds a compact, server-composed Partner Desk section navigator inside the selected detail. Each item carries the canonical availability reason and opens the existing permissioned read view; it is not a second configuration authority. Services & Operations now includes a read-only Supply & Media status panel with inventory, availability, rate-plan and active-media counts plus per-service applicability/setup state. Inventory & Rates and Media Library remain reachable through the same detail view, with all mutation controls disabled in Admin mode.

The Backend command contract now treats `partner_verification.read` as sufficient for Admin read-only Inventory & Rates/Media Library navigation while retaining separate Partner `supply.write` and `media.write` authorization. A focused regression covers Admin read navigation, wrong-permission denial and no Partner write grant. Supply refresh failures remain truthful: stale values are labelled and a bounded “Try again” action is provided; no zero or optimistic success is shown.

Evidence: Backend commit `5a1e286` (focused command-center suite 16/16 PASS, TypeScript and build PASS; staged artifact applied to `tpl-api-partner-staging` `/home/tpladmin/tpl-api-releases/partner-m1m2-rbac-3b5f82e`, `/api/v1/health` HTTP 200). Website commit `eb4b598`; clean Webpack production build PASS, scoped ESLint 0 errors/2 pre-existing accessibility warnings, Preview `dpl_4fGfxwvHqwQi2RKS2vHV8qFL7dQ9` READY and `staging.tplgo.com` points to it. The operator confirmed the section map and Supply & Media status read-only after refresh. Production aliases/processes and Partner data were not changed.

The referenced complete-desk roadmap file was absent at batch start. The operator's supplied instruction was used as the approved direction and saved as a supporting roadmap; it is not another master. The unified direction lock now records this refinement. Production and unrelated organizations, applications, catalogue and recovery execution were not changed.

## Source and deployed baseline

| Surface | Scoped source and delivery |
|---|---|
| Backend | Foundation `2bc28d8e0216746d2e74d35fabe9d4d24f335082`, permission fix `3b5f82e5235c27e3bc91b65da1e1610e4b0c1ce6`; current staging release `/home/tpladmin/tpl-api-releases/partner-m1m2-rbac-3b5f82e`; only `tpl-api-partner-staging`/4100 switched. Local/public staging health 200; unauthenticated supply endpoint 401. |
| Website | Foundation `0973430540bec6a45a63e2e1d491e7c15db2013a`, permission fix `30cc82c0be52c221152a13286b4434eab50010ad`; current clean Vercel Preview `dpl_3KJ9KdnWFPnrGszodzP77o4JrcRy` / `tplgo-website-mzn994cf0-tplgo.vercel.app` READY; `staging.tplgo.com` assigned to that Preview. Protected endpoint redirects to Vercel SSO as expected. |
| Mobile | Foundation `57e520a96e7066133067d8a52c3db5e2297b1241`, permission fix `0d773f50a73a55dd44f5f8b5a21e14d584e0aa90`; existing Development APK/package `com.tplgo.mobile`, app data and Metro retained; corrected Android Hermes JS export passed. No native dependency/configuration change or new APK. Metro may also serve preserved pre-existing working-tree changes, so the device runtime is not claimed to be commit-only. |
| Staging data | Additive migration `0056_partner_supply_media_foundation` applied after a fresh staging-only backup. All five new supply/media tables had zero rows immediately after deployment. No fixture write was made to prove M2 live parity. |

The initial Backend release archive SHA-256 is `e23ab4d5aafc679bc5e95554d097bebffaf3b4966261567a4f0eb8962a1ee1de`; the corrective archive SHA-256 is `e28f2f04d95f726e9025a269e06cfea632e35553210ddf7dcc2aff5063418cfe`. Private staging backup: `/home/tpladmin/tpl-api-releases/partner-m1m2-2bc28d8f/backups/tpl-backup-2026-09-20T14-08-44-515Z.dump`, SHA-256 `a63a52315bd807f67108d7da7fad7d4adc58391dec577d2a90d6fd4c93e31f73`, `pg_restore --list` PASS. Migration 0056 was applied only in the initial release; the corrective release is code-only. The first immediate post-switch probe raced Backend boot and rolled back to UF3.1; a bounded 30-second retry switched the initial release successfully. The later permission-fix release passed its bounded health gate. No production process, alias or database was changed. Rollback uses the prior staging application release/Preview; preserve additive tables and any future recorded supply events for forward repair, not destructive drop.

## M1 availability and navigation

The shared command contract now keeps the approved Partner menus visible and in order. Native More and Website navigation use applicable named Bookings, Orders, Cases and Leads rather than a generic Work destination; Finance and Settlements remain distinct. Server-composed availability explicitly distinguishes `ready_authorized`, `not_applicable_to_service`, `approval_pending`, `setup_incomplete`, `permission_missing` and `implementation_unavailable`. Backend authorization still governs requests and deep links. A focused review found that the initial generic write check treated `supply.write` and `media.write` interchangeably. The corrective release enforces each grant separately, returns `canWriteSupply`/`canWriteMedia`, and hides the corresponding unauthorized Website/native write controls. The regression verifies both denial directions and read-model flags.

| Menu | M1/M2 status |
|---|---|
| Command Center | Canonical status/actions/services reader connected; new availability and Partner-versus-TPL action ownership. |
| My Business | Existing scoped business destination connected; no new profile authority. |
| Services | Published catalogue and actual selected/approved scope reader connected; publication does not grant service entitlement. |
| Inventory & Rates | M2 inventory/capacity, date-only availability and base-rate UI/API for eligible authorized services; live write parity OPEN. |
| Media Library | M2 private image upload/confirm/replace/remove UI/API where applicable; live cross-client upload OPEN. |
| Bookings | Named destination visible; only existing backed capability may open. M2 did not build a booking engine. |
| Orders | Named destination visible; backed capability required, otherwise explicit unavailable/not-applicable reason. |
| Cases | Named destination visible; existing care foundation only, not full Medical operations. |
| Leads | Named destination visible; no new lead engine certified in M2. |
| CRM | Visible with capability/implementation reason; full CRM workflow remains open. |
| Finance | Separate visible destination; full financial operations remain open and no balances are fabricated. |
| Settlements | Separate visible destination; payout/settlement execution remains unavailable. |
| Reports | Bounded supply/service-status PDF/CSV/XLSX and print via existing export foundation; broader analytics/schedules open. |
| Integrations | Visible, truthfully pending/disconnected without verified provider health. |
| Compliance | Existing permissioned destination retained; M2 does not redo specialist review. |
| Support | Visible; unsupported support operations remain explicitly unavailable. |
| Team & Access | Bottom destination visible; no new account provisioning or grants. |
| Settings | Bottom destination visible; no duplicate configuration authority. |

The prior “3 service setups pending” versus “No action required” contradiction is resolved by assigning the pending review/setup task to TPL. Partner Action Required now says no Partner action where none exists, while the separate TPL review state remains visible; no service was activated to populate the screen.

ADB connected to the existing wireless device. The existing TPL app opened without reinstall. Native More visibly showed Media Library, named Bookings/Orders/Leads/Cases, CRM, Finance, Settlements, Reports and Integrations with reasons; the selected service correctly showed Media Library as not applicable and Finance/Settlements as implementation unavailable. This is a scoped native visual observation, not full menu/back/flow or M2 write certification. The captured screen is a transient local QA artifact, not a report attachment because other visible account context was not suitable for public evidence.

## M2 canonical flow and boundaries

Migration `0056` adds Partner-owned supply items, dated availability, rate plans, media assets and supply events. Existing identity, organization, membership, published catalogue and service selection remain authoritative. Supply writes require an active eligible organization/service, scoped membership and write permission. The read model is bounded for Partner and permissioned Admin detail; no unbounded browser dataset is loaded. The Admin Partner detail reuses the established Overview / Services & Operations / Finance / Compliance & Integrations / Activity & Reports structure and opens supply/media read views without a duplicate configuration editor.

Inventory capacity is nonnegative and versioned; availability is date-only with overlap/capacity validation; rate plans are explicit base prices in their own currency, without hidden tax/commission/settlement calculations. Optimistic versions and locks reject stale/concurrent writes and record audit/history. Published service capability controls applicability but never grants activation. Branch/property/facility authority was not established in the existing foundation, so M2 is explicitly organization/service scoped and does not invent branches or commercial rules.

Media uses the existing private signed-upload architecture: JPEG/PNG/WebP only, 15 MB maximum and a technical limit of 100 active items. Session/confirm checks bind owner, organization, media category and provider object. A failed replacement leaves the previous asset intact. Remove is versioned and audited; no public unrestricted folder, arbitrary URL or base64 profile data. Full media processing/variant policy and device-to-Website live photo equality remain open.

Partner and Admin exports use server authorization, bounded allowlisted content and private responses. CSV/XLSX guard against spreadsheet formulas; PDF/print is a service/readiness summary, not a financial ledger. Report equality with actual eligible staging source records is pending the live write gate. Finance amounts with no canonical source remain unavailable.

The UF3.1 care/event contract remains intact. M2 did not add HIS/provider connectivity or complete Medical operations.

## Verification and evidence limits

| Check | Result and boundary |
|---|---|
| Backend | Focused command, supply, private storage and application export suites: 33/33 PASS after the grant-separation regression; TypeScript and build PASS. Supply tests mock the database/storage boundary; no real isolated database write or external upload is claimed. A sandboxed local build first failed to write `dist`; the same build passed with the required filesystem permission, and the clean server release build passed. |
| Website | Scoped ESLint and clean Vercel production build PASS (242 generated static pages). Broad `tsc --noEmit` remains FAIL on pre-existing test declaration/implicit-any errors; the build itself skipped independent type validation. No broad TypeScript PASS claim. |
| Mobile | Focused Jest 19/19 PASS on a justified retry with 15-second timeout; first run had one unchanged rendered test time out at its 5-second default. TypeScript, scoped lint (0 errors, six existing style warnings), Android Hermes export and public-config check PASS. JS-only; existing APK retained. |
| Source hygiene | Scoped secret scan and `git diff --check` PASS for changed source. Unrelated dirty/untracked work remains in all three worktrees and was not staged or overwritten. |
| Staging | Backend local/public health 200; anonymous supply request 401; Website Preview READY, staging alias moved and protected page redirects to SSO. |
| Native device | Connected wireless ADB, installed TPL app opens, selected Partner's native menu/reasons visible. No eligible synthetic M2 write was performed. |
| Authenticated Website/Admin | Operator read-only Website Partner Home PASS: Inventory & Rates and Media Library both visible with correct availability reasons on the existing active organization. Exact Admin M2 view and authenticated export downloads remain NOT VERIFIED live. The supported in-app browser could not initialize (`process` property conflict) and one reset/recovery attempt failed identically; no further browser setup loop was run. A protected HTTP redirect/isolated tests are not browser UI certification. |
| Bidirectional M2 | Website save → Mobile/Admin, Mobile save → Website/Admin, report/export/source equality and media failure/retry remain OPEN for an eligible isolated synthetic fixture. |

No full load test, staging restore drill, physical print test, authenticated export download, cross-client live write, or production test is claimed. Existing UF1 publication-timing, UF2 real reviewer/correction, UF3 activation-policy and full Partner session/regression gates remain open.

## Conserved accounting and forecast

The fixed UF0 denominator stays **46 requirements / 213 relative effort units**. M1 strengthens UF21 shared Partner/Admin workspace subcriteria. M2 adds tested subcriteria to UF22 supply and UF39 bounded export plus security/audit rows; none of these whole requirements is called complete from partial flows. The last audited evidence classification was 41 corroborated + 127 open + 45 unknown = 213; this is not a completion percentage. Implemented/tested, fully end-to-end certified and remaining percentages remain **NOT YET MEASURABLE** until the acceptance numerator and unknowns are reconciled. Live external connector count is unchanged at zero.

Grounded forecast for the **remaining M2 certification only**: approximately 1–3 engineering days for an eligible isolated staging fixture, authenticated Website/native/Admin comparisons, export/media failure checks and any bounded defects, excluding operator login/approval waiting. Confidence is low because no eligible staging write fixture is yet confirmed and live failures may expand scope. The rest of the Partner program (transactions, CRM, finance/settlement, provider connections, full reporting and certification) cannot be credibly reduced to the user's 48–72-hour target from this evidence; each needs its own source/acceptance estimate. This forecast is engineering time, not elapsed calendar time or a completion claim.

## Next action

Perform one authenticated read-only M1 screen/availability parity pass across Website Partner Home, Mobile and Admin Partner detail. Then provision or identify an authorized **isolated eligible synthetic active service** through supported staging flows, and certify Website and Mobile supply/media writes, Admin readback and export equality without touching the separate unrelated active organization. Keep `MOBILE_USER_PARITY=COMPLETE`, `PARTNER_MOBILE_PARITY=OPEN`, and `PHASE_1_STEP_1=OPEN`. Do not start the next operational module or production release on the basis of menu visibility.

## Single-master verification

The actual tracker is `C:\Users\Admin\tpl-api\reports\TPL_MASTER_REMAINING_WORK_LOG.md`. The unique heading is `Partner Complete Desk M1/M2 checkpoint — 2026-09-20`; the checkpoint ID is `PARTNER_DESK_M1_M2_STRUCTURE_SUPPLY_CHECKPOINT`. Previous current status was `UF3_1_IMPLEMENTED_TESTED_STAGING_DELIVERED_AUTHENTICATED_LIVE_GATES_OPEN`; current status is `PARTNER_DESK_M1_M2_IMPLEMENTED_STAGING_DELIVERED_LIVE_WRITE_PARITY_OPEN`. Heading, checkpoint ID and new-current-status each occur once; matching master-file count is one. `git diff --check` exits 0 and the actual master diff contains the checkpoint. The master was already modified before this batch; its full diff includes preserved earlier uncommitted checkpoints and is not represented as solely M1/M2 work. No duplicate master was created.
