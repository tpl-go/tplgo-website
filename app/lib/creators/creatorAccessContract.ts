import type { AuthUser } from "@/app/lib/auth/auth.types";
export const CREATOR_ACCESS_CONTRACT_VERSION = "creator-access.v1" as const;
export type CreatorAccessStatus = "not_creator"|"onboarding_draft"|"submitted"|"under_review"|"approved"|"changes_requested"|"rejected"|"suspended"|"deactivated";
export type CreatorAccessDTO = { version:typeof CREATOR_ACCESS_CONTRACT_VERSION; userId:string; creatorId:string|null; status:CreatorAccessStatus; onboardingCompleted:boolean; studioAccessAllowed:boolean; publicProfileAllowed:boolean; uploadAllowed:boolean; payoutAllowed:boolean; moderationState:string; reason:string|null; updatedAt:string|null; source:"testing_config"|"testing_api"|"unavailable"; requestId?:string };
const allowedStatuses = new Set<CreatorAccessStatus>(["not_creator","onboarding_draft","submitted","under_review","approved","changes_requested","rejected","suspended","deactivated"]);
export function resolveCreatorAccess(user:AuthUser|null):CreatorAccessDTO { const configured=process.env.NEXT_PUBLIC_TPL_CREATOR_TEST_STATUS; const status=(configured&&allowedStatuses.has(configured as CreatorAccessStatus)?configured:"not_creator") as CreatorAccessStatus; const approved=status==="approved"; return {version:CREATOR_ACCESS_CONTRACT_VERSION,userId:user?.id??"",creatorId:approved?`creator-${user?.id??"test"}`:null,status,onboardingCompleted:["submitted","under_review","approved","changes_requested","rejected","suspended","deactivated"].includes(status),studioAccessAllowed:approved,publicProfileAllowed:approved,uploadAllowed:approved,payoutAllowed:false,moderationState:status,reason:status==="rejected"?"Creator application was not approved.":status==="suspended"?"Creator Studio access is suspended.":status==="deactivated"?"Creator Mode is deactivated.":null,updatedAt:null,source:"testing_config"}; }
export function creatorAccessDestination(status:CreatorAccessStatus){if(status==="not_creator")return "/creators/become-a-creator";if(["onboarding_draft","submitted","under_review","changes_requested"].includes(status))return "/creators/onboarding";return null;}
