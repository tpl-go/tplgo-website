import Image from "next/image";
import Link from "next/link";
import { BadgeCheck, MapPin, Star } from "lucide-react";
import { marketplaceHomeCreators } from "@/app/lib/creators/creatorMarketplaceHomeData";

export default function CreatorFeaturedCreators() {
  return <section className="py-10 sm:py-12"><div className="mx-auto max-w-[1440px] px-4 sm:px-6 lg:px-10"><div className="flex items-end justify-between"><div><h2 className="text-2xl font-black tracking-tight">Featured Creators</h2><p className="mt-1 text-sm font-medium text-slate-500">Top creators delivering world class content</p></div><Link href="/creators/authors" className="hidden text-xs font-black text-blue-700 sm:block">View all creators →</Link></div><div className="mt-6 grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{marketplaceHomeCreators.map((creator) => <article key={creator.slug} className="rounded-xl border border-slate-200 bg-white p-4 shadow-sm transition hover:-translate-y-0.5 hover:shadow-lg"><div className="flex items-center gap-3"><Image src={creator.avatar} alt={creator.name} width={48} height={48} className="h-12 w-12 rounded-full object-cover ring-2 ring-blue-100" /><div className="min-w-0"><h3 className="flex items-center gap-1 truncate text-sm font-black">{creator.name}<BadgeCheck className="h-3.5 w-3.5 shrink-0 text-blue-600" /></h3><p className="mt-0.5 truncate text-[10px] font-semibold text-slate-500">{creator.specialty}</p><p className="mt-1 flex items-center gap-1 text-[10px] text-slate-400"><MapPin className="h-3 w-3" />{creator.location}</p></div></div><div className="mt-3 grid grid-cols-3 gap-1.5">{creator.portfolio.map((src, index) => <div key={`${creator.slug}-${index}`} className="relative aspect-[4/3] overflow-hidden rounded-md bg-slate-100"><Image src={src} alt="" fill sizes="120px" className="object-cover" /></div>)}</div><div className="mt-3 grid grid-cols-3 divide-x divide-slate-200 text-center text-[9px] font-semibold text-slate-500"><span><b className="block text-xs text-slate-900">{creator.assetCount}</b>Assets</span><span><b className="block text-xs text-slate-900">{creator.salesLabel.split(" ")[0]}</b>Followers</span><span><b className="flex items-center justify-center gap-0.5 text-xs text-slate-900"><Star className="h-3 w-3 fill-amber-400 text-amber-400" />{creator.rating}</b>Rating</span></div><Link href={`/creators/authors/${creator.slug}`} className="mt-4 block rounded-md border border-slate-300 py-2 text-center text-[11px] font-black hover:border-blue-600 hover:text-blue-700">View Profile</Link></article>)}</div></div></section>;
}
