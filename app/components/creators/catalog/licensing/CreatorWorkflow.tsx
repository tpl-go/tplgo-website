import { ArrowRight } from "lucide-react";
import { commercialWorkflow } from "@/app/lib/creators/creatorLicensingData";

export default function CreatorWorkflow() {
  return <section className="mx-auto max-w-[1440px] px-4 py-14 sm:px-6 lg:px-10"><div className="mx-auto max-w-3xl text-center"><p className="text-xs font-bold uppercase tracking-[0.16em] text-blue-700">From discovery to deployment</p><h2 className="mt-2">Commercial workflow</h2><p className="mt-3 font-medium text-slate-600">A transparent six-step path keeps creative teams moving and usage rights documented.</p></div><ol className="mt-9 grid gap-3 sm:grid-cols-2 lg:grid-cols-6">{commercialWorkflow.map(({ label, icon: Icon }, index) => <li key={label} className="relative flex min-h-32 flex-col items-center justify-center rounded-2xl border border-slate-200 bg-white p-4 text-center shadow-sm"><span className="grid h-11 w-11 place-items-center rounded-xl bg-blue-50 text-blue-700"><Icon className="h-5 w-5" /></span><strong className="mt-3 text-sm font-bold">{index + 1}. {label}</strong>{index < commercialWorkflow.length - 1 && <ArrowRight className="absolute -right-3 top-1/2 z-10 hidden h-5 w-5 -translate-y-1/2 rounded-full bg-white text-blue-600 lg:block" />}</li>)}</ol><p className="mt-4 text-center text-xs font-semibold text-slate-600">Workflow preview only — purchase, certificate generation and download execution are disabled.</p></section>;
}
