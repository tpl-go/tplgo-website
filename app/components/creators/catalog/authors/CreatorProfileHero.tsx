"use client";
import Image from "next/image"; import { BadgeCheck, Mail, MapPin, Share2, Star, UserPlus } from "lucide-react"; import type { CreatorAuthorProfile } from "@/app/lib/creators/creatorAuthorPageTypes";
const compact = (value: number) => new Intl.NumberFormat("en", { notation: "compact", maximumFractionDigits: 1 }).format(value);
export default function CreatorProfileHero({ creator, followed, notice, onFollow, onShare, onContact }: { creator: CreatorAuthorProfile; followed: boolean; notice: string; onFollow: () => void; onShare: () => void; onContact: () => void }) { const metrics = [[compact(creator.followers), "Followers"], [String(creator.assetCount), "Assets"], [compact(creator.downloadCount), "Downloads"], [creator.rating.toFixed(1), "Rating"]]; return <section className="bg-white"><div className="relative h-52 overflow-hidden bg-[#071831] sm:h-64"><Image src={creator.coverImage} alt="" fill priority sizes="100vw" className="object-cover opacity-80" /><div className="absolute inset-0 bg-gradient-to-t from-[#071831]/80 to-transparent" /></div><div className="mx-auto max-w-[1440px] px-4 sm:px-6 lg:px-10"><div className="relative pb-7"><Image src={creator.avatar} alt={creator.displayName} width={128} height={128} className="-mt-16 h-28 w-28 rounded-2xl border-4 border-white object-cover shadow-xl sm:h-32 sm:w-32" /><div className="mt-4 flex flex-col gap-5 lg:flex-row lg:items-start lg:justify-between"><div className="max-w-3xl"><div className="flex flex-wrap items-center gap-2"><h1 className="text-3xl font-black">{creator.displayName}</h1>{creator.verified && <BadgeCheck className="h-5 w-5 text-blue-600" />}<span className="rounded-full bg-blue-50 px-2.5 py-1 text-[9px] font-black text-blue-700">{creator.creatorLevel}</span></div><p className="mt-1 text-xs font-bold text-slate-500">{creator.username} · {creator.specialization}</p><p className="mt-2 inline-flex items-center gap-1 text-[10px] font-semibold text-slate-500"><MapPin className="h-3.5 w-3.5" />{creator.location}</p><p className="mt-3 text-sm font-medium leading-6 text-slate-600">{creator.bio}</p><p className="mt-2 inline-flex items-center gap-1 text-[10px] font-bold text-amber-700"><Star className="h-3.5 w-3.5 fill-current" />{creator.rating} ({creator.reviewCount} reviews) · Member since {creator.joinedAt}</p></div><div className="flex flex-wrap gap-2"><button onClick={onFollow} className={`inline-flex h-10 items-center gap-2 rounded-md px-4 text-[10px] font-black ${followed ? "bg-blue-50 text-blue-700" : "bg-blue-600 text-white"}`}><UserPlus className="h-4 w-4" />{followed ? "Following" : "Follow"}</button><button onClick={onContact} className="inline-flex h-10 items-center gap-2 rounded-md border border-slate-300 px-4 text-[10px] font-black"><Mail className="h-4 w-4" />Contact / Licensing</button><button aria-label="Share profile" onClick={onShare} className="grid h-10 w-10 place-items-center rounded-md border border-slate-300"><Share2 className="h-4 w-4" /></button></div></div><div className="mt-6 grid grid-cols-2 gap-2 sm:grid-cols-4">{metrics.map(([value, label]) => <div key={label} className="rounded-lg border border-slate-200 bg-slate-50 px-4 py-3"><b className="block text-lg">{value}</b><span className="text-[9px] font-semibold text-slate-500">{label}</span></div>)}</div>{notice && <p className="mt-3 text-[10px] font-bold text-blue-700">{notice}</p>}</div></div></section>; }
