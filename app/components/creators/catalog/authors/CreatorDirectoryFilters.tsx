import { RotateCcw } from "lucide-react"; import type { ReadonlyURLSearchParams } from "next/navigation";
const groups = [{ key: "specialization", title: "Specialization", options: ["Photography", "Videography", "Drone", "Illustration", "Templates", "Presets & LUTs", "Graphics", "Writing & Guides"] }, { key: "country", title: "Location", options: ["India", "United States", "United Kingdom", "Canada", "Germany", "Australia", "Other"] }, { key: "level", title: "Creator Level", options: ["Rising Creator", "Professional", "Elite", "Enterprise Studio"] }, { key: "rating", title: "Rating", options: ["4", "4.5", "top"] }, { key: "followers", title: "Followers", options: ["under-10k", "10k-50k", "50k-100k", "100k+"] }, { key: "assets", title: "Asset Count", options: ["under-100", "100-500", "500-1000", "1000+"] }];
export default function CreatorDirectoryFilters({ params, onUpdate, onClear }: { params: ReadonlyURLSearchParams; onUpdate: (patch: Record<string, string | null>) => void; onClear: () => void }) { const body = <div className="divide-y divide-slate-100">{groups.map((group) => <details open key={group.key} className="py-3"><summary className="cursor-pointer text-[11px] font-black">{group.title}</summary><div className="mt-2 space-y-2">{group.options.map((option) => <button type="button" role="radio" aria-checked={params.get(group.key) === option} key={option} onClick={() => onUpdate({ [group.key]: params.get(group.key) === option ? null : option })} className="flex w-full items-center gap-2 text-left text-[9px] font-semibold text-slate-600"><span className={`h-3 w-3 rounded-full border ${params.get(group.key) === option ? "border-4 border-blue-600" : "border-slate-300"}`} />{option}</button>)}</div></details>)}<label className="flex items-center justify-between py-4 text-[10px] font-black">Verified only<button type="button" role="switch" aria-checked={params.get("verified") === "true"} onClick={() => onUpdate({ verified: params.get("verified") === "true" ? null : "true" })} className={`h-5 w-9 rounded-full p-0.5 ${params.get("verified") === "true" ? "bg-blue-600" : "bg-slate-300"}`}><span className={`block h-4 w-4 rounded-full bg-white transition ${params.get("verified") === "true" ? "translate-x-4" : ""}`} /></button></label></div>; return <><details className="rounded-xl border border-slate-200 bg-white p-4 lg:hidden"><summary className="cursor-pointer text-sm font-black">Creator filters</summary>{body}</details><aside className="hidden h-fit rounded-xl border border-slate-200 bg-white p-4 lg:sticky lg:top-20 lg:block"><div className="flex items-center justify-between"><h2 className="text-sm font-black">Filters</h2><button type="button" onClick={onClear} className="inline-flex items-center gap-1 text-[9px] font-black text-blue-700"><RotateCcw className="h-3 w-3" />Clear all</button></div>{body}</aside></>; }
