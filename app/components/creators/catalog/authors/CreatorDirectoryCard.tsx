"use client";
import Image from "next/image"; import Link from "next/link"; import { BadgeCheck, Heart, MapPin, Star } from "lucide-react"; import { useState } from "react"; import type { CreatorAuthorProfile } from "@/app/lib/creators/creatorAuthorPageTypes";
const compact = (value: number) => new Intl.NumberFormat("en", { notation: "compact", maximumFractionDigits: 1 }).format(value);
export default function CreatorDirectoryCard({ creator, view = "grid" }: { creator: CreatorAuthorProfile; view?: "grid" | "list" }) { const [liked, setLiked] = useState(false); return <article className={`group overflow-hidden rounded-xl border border-slate-200 bg-white shadow-sm transition hover:-translate-y-0.5 hover:shadow-xl ${view === "list" ? "sm:grid sm:grid-cols-[260px_1fr]" : ""}`}><div className="relative h-28 overflow-hidden bg-slate-200"><Image src={creator.coverImage} alt="" fill sizes="(min-width:1024px) 28vw, 100vw" className="object-cover transition duration-500 group-hover:scale-105" /><div className="absolute inset-0 bg-gradient-to-t from-[#071831]/45 to-transparent" /><button type="button" aria-label={`Follow ${creator.displayName}`} onClick={() => setLiked((value) => !value)} className="absolute right-3 top-3 grid h-8 w-8 place-items-center rounded-full bg-white shadow"><Heart className={`h-4 w-4 ${liked ? "fill-rose-600 text-rose-600" : "text-slate-700"}`} /></button></div><div className="relative px-4 pb-4"><Image src={creator.avatar} alt={creator.displayName} width={68} height={68} className="-mt-9 h-[68px] w-[68px] rounded-full border-4 border-white object-cover shadow" /><div className="mt-2 flex items-center gap-1"><Link href={`/creators/authors/${creator.slug}`} className="text-sm font-black hover:text-blue-700">{creator.displayName}</Link>{creator.verified && <BadgeCheck className="h-4 w-4 text-blue-600" />}</div><p className="text-[9px] font-bold text-slate-400">{creator.username}</p><p className="mt-2 text-[11px] font-black text-slate-700">{creator.specialization} · {creator.creatorLevel}</p><p className="mt-1 inline-flex items-center gap-1 text-[9px] font-semibold text-slate-500"><MapPin className="h-3 w-3" />{creator.location}</p><div className="mt-3 grid grid-cols-3 gap-2 border-y border-slate-100 py-3 text-center text-[9px] text-slate-500"><span><b className="block text-[11px] text-slate-900">{compact(creator.followers)}</b>Followers</span><span><b className="block text-[11px] text-slate-900">{creator.assetCount}</b>Assets</span><span><b className="block text-[11px] text-slate-900">{compact(creator.downloadCount)}</b>Downloads</span></div><div className="mt-3 flex items-center gap-2 text-[9px] font-bold"><span className="inline-flex items-center gap-1 text-amber-700"><Star className="h-3 w-3 fill-current" />{creator.rating}</span><span className="text-slate-400">({creator.reviewCount} reviews)</span></div><div className="mt-3 flex flex-wrap gap-1.5">{creator.tags.slice(0, 3).map((tag) => <span key={tag} className="rounded-full bg-slate-100 px-2 py-1 text-[8px] font-bold text-slate-600">{tag}</span>)}</div><Link href={`/creators/authors/${creator.slug}`} className="mt-4 block rounded-md bg-[#071831] py-2.5 text-center text-[10px] font-black text-white">View Profile</Link></div></article>; }
