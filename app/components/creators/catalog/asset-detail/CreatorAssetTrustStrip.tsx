import { BadgeCheck, Headphones, ShieldCheck, Sparkles } from "lucide-react";

const items = [[ShieldCheck, "Secure & Trusted", "Transaction-ready safeguards"], [BadgeCheck, "License Certificate", "Certificate-ready licensing"], [Sparkles, "Quality Guaranteed", "Moderated asset previews"], [Headphones, "Support Ready", "Support-ready marketplace experience"]] as const;
export default function CreatorAssetTrustStrip() { return <section className="bg-slate-50 px-4 py-8"><div className="mx-auto grid max-w-[1360px] gap-4 rounded-xl border border-slate-200 bg-white p-5 sm:grid-cols-2 lg:grid-cols-4">{items.map(([Icon, title, copy]) => <div key={title} className="flex items-center gap-3"><span className="grid h-9 w-9 shrink-0 place-items-center rounded-full bg-blue-50 text-blue-700"><Icon className="h-4 w-4" /></span><span><strong className="block text-[11px]">{title}</strong><small className="text-[9px] font-medium text-slate-500">{copy}</small></span></div>)}</div></section>; }
