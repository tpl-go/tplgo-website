import { Check } from "lucide-react";
import { onboardingSteps } from "@/app/lib/creators/creatorOnboardingData";

export default function CreatorStepper({ current }: { current: number }) {
  const currentStep = onboardingSteps[current];
  return <div className="sticky top-14 z-30 border-b border-slate-200 bg-white/95 shadow-sm backdrop-blur"><div className="mx-auto max-w-[1440px] px-4 py-3 sm:px-6 lg:px-10"><div className="flex items-center justify-between gap-4 lg:hidden"><div><p className="text-xs font-bold uppercase tracking-wide text-blue-700">Step {current + 1} of {onboardingSteps.length}</p><p className="mt-1 text-sm font-bold text-slate-950">{currentStep.label}</p></div><div className="h-2 w-32 overflow-hidden rounded-full bg-slate-200"><div className="h-full rounded-full bg-blue-600 transition-all" style={{ width: `${((current + 1) / onboardingSteps.length) * 100}%` }} /></div></div><ol className="hidden items-start lg:flex">{onboardingSteps.map((step, index) => <li key={step.key} className="flex min-w-0 flex-1 items-start"><div className="flex min-w-0 flex-col items-center text-center"><span className={`grid h-8 w-8 place-items-center rounded-full text-xs font-bold ${index < current ? "bg-emerald-600 text-white" : index === current ? "bg-blue-600 text-white ring-4 ring-blue-100" : "bg-slate-100 text-slate-500"}`}>{index < current ? <Check className="h-4 w-4" /> : index + 1}</span><span className={`mt-1.5 text-xs font-semibold ${index === current ? "text-blue-700" : index < current ? "text-emerald-700" : "text-slate-500"}`}>{step.label}</span></div>{index < onboardingSteps.length - 1 && <span className={`mt-4 h-0.5 flex-1 ${index < current ? "bg-emerald-500" : "bg-slate-200"}`} />}</li>)}</ol></div></div>;
}
