import type { CreatorOnboardingDraft } from "@/app/lib/creators/creatorOnboardingData";
import { Field, SelectField, StepIntro, TextareaField } from "./CreatorOnboardingFields";

export default function CreatorProfileStep({ data, errors, update }: { data: CreatorOnboardingDraft; errors: Record<string, string>; update: (patch: Partial<CreatorOnboardingDraft>) => void }) {
  return <div><StepIntro eyebrow="Step 3 · Public presence" title="Create your creator profile" copy="Tell buyers who you are, what you create and where your creative perspective comes from." /><div className="mt-8 grid gap-5 rounded-2xl border border-slate-200 bg-white p-5 shadow-sm sm:p-7 md:grid-cols-2"><Field label="Display name" value={data.displayName} onChange={(event) => update({ displayName: event.target.value })} error={errors.displayName} placeholder="Aira Studio" /><Field label="Username" value={data.username} onChange={(event) => update({ username: event.target.value })} error={errors.username} placeholder="aira-studio" help="Lowercase letters, numbers and hyphens work best." /><div className="md:col-span-2"><TextareaField label="Bio" value={data.bio} onChange={(event) => update({ bio: event.target.value })} error={errors.bio} placeholder="Describe your creative work, experience and point of view." /></div><Field label="Languages" value={data.languages} onChange={(event) => update({ languages: event.target.value })} placeholder="English, Hindi" /><Field label="Location" value={data.location} onChange={(event) => update({ location: event.target.value })} error={errors.location} placeholder="Jaipur, India" /><SelectField label="Primary category" value={data.primaryCategory} error={errors.primaryCategory} onChange={(event) => update({ primaryCategory: event.target.value })}><option value="">Select a category</option><option>Photography</option><option>Video</option><option>Drone</option><option>Templates</option><option>Presets</option><option>Graphics</option></SelectField><SelectField label="Secondary category" value={data.secondaryCategory} onChange={(event) => update({ secondaryCategory: event.target.value })}><option value="">Optional</option><option>Photography</option><option>Video</option><option>Drone</option><option>Templates</option><option>Presets</option></SelectField><Field label="Website" type="url" value={data.website} onChange={(event) => update({ website: event.target.value })} placeholder="https://yourportfolio.com" /><Field label="Social links" value={data.socialLinks} onChange={(event) => update({ socialLinks: event.target.value })} placeholder="Instagram, Vimeo or Behance" /></div></div>;
}
