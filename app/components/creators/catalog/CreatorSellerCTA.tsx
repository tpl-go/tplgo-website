import Image from "next/image";
import { ArrowRight, BadgeIndianRupee, Globe2, ImagePlus, SlidersHorizontal } from "lucide-react";

const items = [[ImagePlus, "Photos, videos and more"], [SlidersHorizontal, "Set your terms and price"], [Globe2, "Global audience"], [BadgeIndianRupee, "Get paid every time"]] as const;

export default function CreatorSellerCTA({ onStudio }: { onStudio: () => void }) {
  return <section className="bg-slate-50 py-12"><div className="mx-auto grid max-w-[1360px] overflow-hidden rounded-2xl border border-blue-100 bg-white shadow-xl shadow-blue-950/5 lg:grid-cols-[1.1fr_.9fr]"><div className="p-7 sm:p-10"><span className="text-[10px] font-black uppercase tracking-[0.18em] text-blue-700">Become a TPL Creator</span><h2 className="mt-3 max-w-2xl text-2xl font-black tracking-tight sm:text-3xl">Share your vision. Earn from your creativity.</h2><p className="mt-3 max-w-2xl text-sm font-medium leading-6 text-slate-600">Upload photos, videos, reels, drone footage, templates and presets. Build your storefront with the same trusted TPL account.</p><div className="mt-6 grid gap-3 sm:grid-cols-2">{items.map(([Icon, title]) => <div key={title} className="flex items-center gap-2.5 text-xs font-bold"><Icon className="h-4 w-4 text-blue-700" />{title}</div>)}</div><div className="mt-7 flex flex-wrap gap-3"><button type="button" onClick={onStudio} className="inline-flex h-10 items-center gap-2 rounded-md bg-blue-600 px-5 text-xs font-black text-white hover:bg-blue-700">Start Uploading <ArrowRight className="h-4 w-4" /></button><button type="button" onClick={onStudio} className="h-10 rounded-md border border-slate-300 px-5 text-xs font-black hover:border-blue-600 hover:text-blue-700">Explore Creator Studio</button></div><p className="mt-3 text-[10px] font-semibold text-slate-400">Uploads, payouts and publishing remain gated during public preview.</p></div><div className="relative min-h-[280px] overflow-hidden bg-[#071831]"><Image src="/experiences/adventure.jpg" alt="Creator reviewing travel media on location" fill sizes="(min-width:1024px) 40vw, 100vw" className="object-cover opacity-80" /><div className="absolute inset-0 bg-gradient-to-r from-white via-transparent to-transparent lg:from-white/20" /><div className="absolute bottom-6 right-6 w-48 rounded-xl border border-white/20 bg-[#071831]/80 p-3 text-white shadow-2xl backdrop-blur"><div className="relative aspect-video overflow-hidden rounded-md"><Image src="/experiences/roadtrip.jpg" alt="Creator asset preview" fill sizes="192px" className="object-cover" /></div><p className="mt-2 text-[10px] font-black">Your work. Your storefront.</p><p className="mt-0.5 text-[9px] text-slate-300">One library across every device.</p></div></div></div></section>;
}
