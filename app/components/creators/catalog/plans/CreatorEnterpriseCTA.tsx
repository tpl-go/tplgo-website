import { ArrowRight, CheckCircle2 } from "lucide-react";
import { enterpriseAudiences } from "@/app/lib/creators/creatorPlansData";

export default function CreatorEnterpriseCTA({ notice, onContact }: { notice: string; onContact: () => void }) {
  return <section className="mx-auto max-w-[1440px] px-4 pb-14 sm:px-6 lg:px-10"><div className="relative overflow-hidden rounded-3xl bg-[#071831] p-6 text-white shadow-2xl sm:p-9 lg:p-12"><div className="absolute -right-20 -top-20 h-72 w-72 rounded-full bg-blue-500/20 blur-3xl" /><div className="relative grid gap-8 lg:grid-cols-[1fr_.8fr] lg:items-center"><div><p className="text-xs font-bold uppercase tracking-[0.16em] text-blue-200">Enterprise solutions</p><h2 className="mt-2 text-white">Need something custom?</h2><p className="mt-4 max-w-2xl font-medium text-slate-200">Custom download volumes, commercial rights, governance and support for large organizations, tourism boards, media houses, hotel groups and government projects.</p><button type="button" onClick={onContact} className="mt-6 inline-flex min-h-11 items-center gap-2 rounded-lg bg-blue-600 px-6 py-3 font-bold text-white hover:bg-blue-500">Contact Sales Team <ArrowRight className="h-4 w-4" /></button>{notice && <p role="status" className="mt-3 text-sm font-semibold text-white">{notice}</p>}</div><div className="grid gap-3 sm:grid-cols-2">{enterpriseAudiences.map((item) => <div key={item} className="flex items-center gap-2 rounded-xl border border-white/15 bg-white/10 px-4 py-3 text-sm font-semibold text-slate-100"><CheckCircle2 className="h-4 w-4 shrink-0 text-blue-300" />{item}</div>)}</div></div></div></section>;
}
