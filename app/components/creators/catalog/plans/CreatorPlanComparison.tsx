import { Check, Minus } from "lucide-react";
import { comparisonKeys, creatorPlans, planComparisonRows, type PlanKey } from "@/app/lib/creators/creatorPlansData";

function Value({ value }: { value: string | boolean }) { if (value === true) return <span className="inline-flex items-center gap-1 font-semibold text-emerald-700"><Check className="h-4 w-4" /> Yes</span>; if (value === false) return <span className="inline-flex items-center gap-1 font-medium text-slate-500"><Minus className="h-4 w-4" /> No</span>; return <span className="font-semibold text-blue-700">{value}</span>; }

export default function CreatorPlanComparison() {
  return <section id="plan-comparison" className="scroll-mt-24 bg-[#f4f7fb] py-14"><div className="mx-auto max-w-[1540px] px-4 sm:px-6 lg:px-8"><div className="max-w-3xl"><p className="text-xs font-bold uppercase tracking-[0.16em] text-blue-700">Feature-by-feature</p><h2 className="mt-2">Compare every plan</h2><p className="mt-3 font-medium text-slate-600">Choose by download volume, licensing, creator tools, team access and service level.</p></div><div className="mt-8 hidden overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm lg:block"><table className="w-full border-collapse text-left"><thead><tr className="bg-[#071831] text-white"><th className="px-4 py-4 text-sm font-bold">Feature</th>{creatorPlans.map((plan) => <th key={plan.key} className="px-3 py-4 text-xs font-bold">{plan.name}</th>)}</tr></thead><tbody>{planComparisonRows.map((row, index) => <tr key={row.feature} className={index % 2 ? "bg-slate-50" : "bg-white"}><th className="border-t border-slate-200 px-4 py-3 text-sm font-semibold">{row.feature}</th>{comparisonKeys.map((key) => <td key={key} className="border-t border-slate-200 px-3 py-3 text-xs"><Value value={row[key]} /></td>)}</tr>)}</tbody></table></div><div className="mt-7 grid gap-4 sm:grid-cols-2 lg:hidden">{creatorPlans.map((plan) => <details key={plan.key} className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><summary className="cursor-pointer text-lg font-extrabold text-slate-950">{plan.name}</summary><dl className="mt-4 divide-y divide-slate-200">{planComparisonRows.map((row) => <div key={row.feature} className="flex items-center justify-between gap-3 py-3"><dt className="text-sm font-medium text-slate-700">{row.feature}</dt><dd className="shrink-0 text-right text-sm"><Value value={row[plan.key as PlanKey]} /></dd></div>)}</dl></details>)}</div></div></section>;
}
