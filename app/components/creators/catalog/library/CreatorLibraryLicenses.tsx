import Link from "next/link";
import { FileBadge2 } from "lucide-react";
import { libraryLicenses } from "@/app/lib/creators/creatorLibraryData";

export default function CreatorLibraryLicenses() {
  return <div className="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div className="hidden overflow-x-auto md:block"><table className="w-full min-w-[780px] border-collapse text-left"><thead><tr className="bg-[#071831] text-white">{["Asset", "License", "License ID", "Purchase Date", "Expiry", "Status"].map((item) => <th key={item} className="px-4 py-4 text-xs font-bold">{item}</th>)}</tr></thead><tbody>{libraryLicenses.map((item, index) => <tr key={item.id} className={index % 2 ? "bg-slate-50" : "bg-white"}><td className="border-t border-slate-200 px-4 py-4"><Link href={`/creators/assets/${item.assetSlug}`} className="text-sm font-bold text-slate-950 hover:text-blue-700">{item.asset}</Link></td><td className="border-t border-slate-200 px-4 py-4 text-sm font-semibold text-blue-700">{item.license}</td><td className="border-t border-slate-200 px-4 py-4 text-xs font-semibold text-slate-600">{item.id}</td><td className="border-t border-slate-200 px-4 py-4 text-xs font-medium text-slate-600">{item.purchaseDate}</td><td className="border-t border-slate-200 px-4 py-4 text-xs font-medium text-slate-600">{item.expiry}</td><td className="border-t border-slate-200 px-4 py-4"><span className="rounded-full bg-emerald-100 px-2.5 py-1 text-xs font-bold text-emerald-800">{item.status}</span></td></tr>)}</tbody></table></div><div className="grid gap-3 p-3 md:hidden">{libraryLicenses.map((item) => <article key={item.id} className="rounded-xl border border-slate-200 p-4"><div className="flex items-start gap-3"><span className="grid h-10 w-10 shrink-0 place-items-center rounded-xl bg-blue-50 text-blue-700"><FileBadge2 className="h-5 w-5" /></span><div className="min-w-0"><Link href={`/creators/assets/${item.assetSlug}`} className="font-bold text-slate-950">{item.asset}</Link><p className="mt-1 text-xs font-semibold text-blue-700">{item.license} · {item.status}</p></div></div><dl className="mt-4 grid grid-cols-2 gap-3 border-t border-slate-100 pt-3 text-xs"><div><dt className="font-medium text-slate-500">License ID</dt><dd className="mt-1 font-semibold text-slate-800">{item.id}</dd></div><div><dt className="font-medium text-slate-500">Purchase date</dt><dd className="mt-1 font-semibold text-slate-800">{item.purchaseDate}</dd></div><div><dt className="font-medium text-slate-500">Expiry</dt><dd className="mt-1 font-semibold text-slate-800">{item.expiry}</dd></div><div><dt className="font-medium text-slate-500">Status</dt><dd className="mt-1 font-semibold text-emerald-700">{item.status}</dd></div></dl></article>)}</div></div>;
}
