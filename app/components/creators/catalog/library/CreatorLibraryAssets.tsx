import Image from "next/image";
import Link from "next/link";
import { BadgeCheck, Download, Ellipsis, Eye } from "lucide-react";
import { creatorAssets } from "@/app/lib/creators/creatorCatalogData";
import { marketplaceHomeAssets } from "@/app/lib/creators/creatorMarketplaceHomeData";
import { libraryAssets } from "@/app/lib/creators/creatorLibraryData";

export default function CreatorLibraryAssets({ mode, limit, onPreview }: { mode: "downloads" | "saved"; limit?: number; onPreview: (message: string) => void }) {
  const records = libraryAssets.filter((item) => mode === "downloads" ? item.state === "Downloaded" : item.state === "Saved").slice(0, limit);
  return <div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-3">{records.map((record) => { const asset = creatorAssets.find((item) => item.slug === record.slug); if (!asset) return null; const home = marketplaceHomeAssets.find((item) => item.slug === asset.slug); const image = home?.previewImage ?? "/themes/banners/culture-1.jpg"; return <article key={asset.slug} className="group overflow-hidden rounded-xl border border-slate-200 bg-white shadow-sm transition hover:-translate-y-0.5 hover:shadow-lg"><div className="relative aspect-[4/3] overflow-hidden bg-slate-100"><Image src={image} alt={asset.title} fill sizes="(min-width:1280px) 25vw, 50vw" className="object-cover transition duration-500 group-hover:scale-105" /><span className="absolute left-3 top-3 rounded bg-[#071831]/90 px-2 py-1 text-xs font-bold uppercase text-white">{asset.mediaType}</span><button type="button" onClick={() => onPreview(`Asset actions preview opened for ${asset.title}.`)} aria-label={`Actions for ${asset.title}`} className="absolute right-3 top-3 grid h-9 w-9 place-items-center rounded-full bg-white text-slate-900 shadow"><Ellipsis className="h-4 w-4" /></button><Link href={`/creators/assets/${asset.slug}`} className="absolute inset-0" aria-label={`Open ${asset.title}`} /></div><div className="p-4"><Link href={`/creators/assets/${asset.slug}`} className="line-clamp-2 font-bold text-slate-950 hover:text-blue-700">{asset.title}</Link><Link href={`/creators/authors/${asset.creatorSlug}`} className="mt-2 inline-flex items-center gap-1 text-sm font-semibold text-slate-600 hover:text-blue-700">{asset.creatorName}<BadgeCheck className="h-3.5 w-3.5 text-blue-600" /></Link><div className="mt-4 flex items-center justify-between gap-3 border-t border-slate-100 pt-3"><div><p className="text-xs font-bold text-blue-700">{record.license} License</p><p className="mt-1 text-xs font-medium text-slate-600">{record.state} · {record.date}</p></div><button type="button" onClick={() => onPreview(mode === "downloads" ? "Download preview only — no file was transferred." : "Saved asset quick action preview opened.")} className="grid h-9 w-9 shrink-0 place-items-center rounded-lg bg-blue-50 text-blue-700" aria-label={mode === "downloads" ? "Preview download action" : "Preview saved asset"}>{mode === "downloads" ? <Download className="h-4 w-4" /> : <Eye className="h-4 w-4" />}</button></div></div></article>; })}</div>;
}
