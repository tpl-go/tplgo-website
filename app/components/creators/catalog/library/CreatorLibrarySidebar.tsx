import { HelpCircle, HardDrive, X } from "lucide-react";
import { libraryNavigation, storagePreview, type LibraryTab } from "@/app/lib/creators/creatorLibraryData";

export default function CreatorLibrarySidebar({ current, userName, open, onClose, onTab, onAction, onUpgrade }: { current: LibraryTab; userName: string; open: boolean; onClose: () => void; onTab: (tab: LibraryTab) => void; onAction: (action: "searches" | "following" | "settings" | "help") => void; onUpgrade: () => void }) {
  const content = <aside className="flex h-full flex-col bg-white"><div className="border-b border-slate-200 p-5"><div className="flex items-start justify-between gap-3"><div><div className="grid h-12 w-12 place-items-center rounded-full bg-[#071831] text-sm font-extrabold text-white">{userName.slice(0, 2).toUpperCase()}</div><h2 className="mt-3 text-lg font-bold">{userName}</h2><span className="mt-2 inline-flex rounded-full bg-violet-100 px-3 py-1 text-xs font-bold text-violet-800">Creator Plan · Preview</span></div><button type="button" onClick={onClose} aria-label="Close library menu" className="grid h-10 w-10 place-items-center rounded-lg border border-slate-200 lg:hidden"><X className="h-5 w-5" /></button></div></div><nav aria-label="Library sections" className="flex-1 space-y-1 overflow-y-auto p-3">{libraryNavigation.map(({ label, tab, action, icon: Icon }) => <button type="button" key={label} onClick={() => { if (tab) onTab(tab); else if (action) onAction(action); onClose(); }} className={`flex min-h-10 w-full items-center gap-3 rounded-lg px-3 py-2 text-left text-sm font-semibold transition ${tab === current ? "bg-blue-50 text-blue-800" : "text-slate-700 hover:bg-slate-100"}`}><Icon className={`h-4 w-4 ${tab === current ? "text-blue-700" : "text-slate-500"}`} />{label}</button>)}</nav><div className="border-t border-slate-200 p-4"><div className="rounded-xl bg-slate-50 p-4"><div className="flex items-center justify-between gap-3"><span className="inline-flex items-center gap-2 text-sm font-bold"><HardDrive className="h-4 w-4 text-blue-700" />Storage</span><span className="text-xs font-semibold text-slate-600">{storagePreview.percent}%</span></div><div className="mt-3 h-2 overflow-hidden rounded-full bg-slate-200"><div className="h-full rounded-full bg-blue-600" style={{ width: `${storagePreview.percent}%` }} /></div><p className="mt-2 text-xs font-medium text-slate-600">{storagePreview.used} of {storagePreview.total} used</p><button type="button" onClick={onUpgrade} className="mt-3 text-xs font-bold text-blue-700">Upgrade storage</button></div><button type="button" onClick={() => onAction("help")} className="mt-3 flex min-h-10 w-full items-center gap-2 rounded-lg px-3 text-sm font-semibold text-slate-700 hover:bg-slate-100"><HelpCircle className="h-4 w-4" />Help Center</button></div></aside>;
  return <><div className="hidden overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm lg:block">{content}</div>{open && <div className="fixed inset-0 z-50 lg:hidden"><button type="button" aria-label="Close library menu backdrop" onClick={onClose} className="absolute inset-0 bg-slate-950/55" /><div className="absolute inset-y-0 left-0 w-[min(88vw,340px)] shadow-2xl">{content}</div></div>}</>;
}
