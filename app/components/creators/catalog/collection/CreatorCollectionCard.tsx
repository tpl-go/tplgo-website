import Image from "next/image";
import Link from "next/link";
import { ArrowRight, BadgeCheck, Images, Users } from "lucide-react";
import type { CreatorCollectionConfig } from "@/app/lib/creators/creatorCollectionPageConfig";
export default function CreatorCollectionCard({ collection }: { collection: CreatorCollectionConfig }) { return <article className="group overflow-hidden rounded-xl border border-slate-200 bg-white shadow-sm transition hover:-translate-y-1 hover:shadow-xl"><Link href={`/creators/collections/${collection.slug}`} className="relative block aspect-[16/10] overflow-hidden"><Image src={collection.thumbnail} alt={collection.title} fill sizes="(min-width:1280px) 25vw, 50vw" className="object-cover transition duration-500 group-hover:scale-105" /><div className="absolute inset-0 bg-gradient-to-t from-[#071831]/75 to-transparent" /><span className="absolute left-3 top-3 rounded bg-white/90 px-2 py-1 text-[8px] font-black uppercase text-slate-800">{collection.group}</span><span className="absolute bottom-3 left-3 rounded bg-blue-600 px-2 py-1 text-[8px] font-black text-white">{collection.updatedLabel}</span></Link><div className="p-4"><h3 className="text-sm font-black">{collection.title}</h3><p className="mt-2 line-clamp-2 min-h-10 text-[10px] font-medium leading-5 text-slate-500">{collection.description}</p><div className="mt-3 flex gap-4 text-[9px] font-bold text-slate-500"><span className="inline-flex items-center gap-1"><Images className="h-3 w-3" />{collection.assetCount} assets</span><span className="inline-flex items-center gap-1"><Users className="h-3 w-3" />{collection.creatorCount} creators</span></div><div className="mt-4 flex items-center justify-between border-t border-slate-100 pt-3"><span className="inline-flex items-center gap-1 text-[9px] font-bold text-blue-700"><BadgeCheck className="h-3 w-3" />Expert curated</span><Link href={`/creators/collections/${collection.slug}`} className="inline-flex items-center gap-1 text-[10px] font-black text-blue-700">View Collection <ArrowRight className="h-3 w-3" /></Link></div></div></article>; }
