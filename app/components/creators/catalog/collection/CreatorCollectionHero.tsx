"use client";
import Image from "next/image"; import { Bookmark, CheckCircle2, Search, Share2 } from "lucide-react"; import { useState } from "react"; import type { CreatorCollectionConfig } from "@/app/lib/creators/creatorCollectionPageConfig";
export default function CreatorCollectionHero({ config, query, saved, notice, onSearch, onTag, onSave, onShare }: { config: CreatorCollectionConfig; query: string; saved: boolean; notice: string; onSearch: (query: string) => void; onTag: (tag: string) => void; onSave: () => void; onShare: () => void }) { const [value, setValue] = useState(query); const stats = [[config.assetCount, "Assets"], [config.creatorCount, "Creators"], [config.categoryCount, "Categories"], [config.updatedLabel.replace("Updated ", ""), "Updated"]]; return <section className="relative isolate overflow-hidden bg-[#071831] text-white"><Image src={config.bannerImage} alt={config.title} fill priority sizes="100vw" className="object-cover opacity-60" /><div className="absolute inset-0 bg-gradient-to-r from-[#06172f] via-[#071831]/85 to-[#071831]/20" /><div className="relative mx-auto grid max-w-[1440px] gap-7 px-4 py-10 sm:px-6 lg:grid-cols-[160px_minmax(0,1fr)] lg:px-10"><div className="relative hidden aspect-square overflow-hidden rounded-xl border-4 border-white/20 shadow-2xl lg:block"><Image src={config.thumbnail} alt="" fill sizes="160px" className="object-cover" /></div><div><div className="flex flex-col gap-4 sm:flex-row sm:items-start sm:justify-between"><div><p className="text-[9px] font-black uppercase tracking-[0.18em] text-blue-200">Premium curated collection</p><h1 className="mt-2 text-3xl font-black tracking-tight sm:text-4xl">{config.title}</h1><p className="mt-2 max-w-2xl text-sm font-medium leading-6 text-slate-200">{config.description}</p><p className="mt-2 inline-flex items-center gap-1.5 text-[10px] font-bold text-blue-200"><CheckCircle2 className="h-3.5 w-3.5" />Curated by {config.curator}</p></div><div className="flex gap-2"><button type="button" onClick={onShare} className="inline-flex h-9 items-center gap-1.5 rounded-md border border-white/25 bg-white/10 px-3 text-[10px] font-black"><Share2 className="h-3.5 w-3.5" />Share Collection</button><button type="button" onClick={onSave} className={`inline-flex h-9 items-center gap-1.5 rounded-md px-3 text-[10px] font-black ${saved ? "bg-blue-600" : "bg-white text-slate-900"}`}><Bookmark className={`h-3.5 w-3.5 ${saved ? "fill-current" : ""}`} />{saved ? "Saved" : "Save Collection"}</button></div></div><div className="mt-5 grid grid-cols-2 gap-2 sm:grid-cols-4">{stats.map(([valueText, label]) => <div key={label} className="rounded-lg border border-white/15 bg-[#071831]/45 px-3 py-2.5 backdrop-blur"><strong className="block text-base font-black">{valueText}</strong><span className="text-[8px] font-semibold text-slate-300">{label}</span></div>)}</div><form onSubmit={(event) => { event.preventDefault(); onSearch(value.trim()); }} className="mt-5 flex max-w-2xl overflow-hidden rounded-lg bg-white p-1"><Search className="ml-3 mt-3 h-4 w-4 text-slate-400" /><input suppressHydrationWarning value={value} onChange={(event) => setValue(event.target.value)} placeholder={config.searchPlaceholder} className="h-10 min-w-0 flex-1 px-3 text-sm font-semibold text-slate-900 outline-none" /><button type="submit" className="rounded-md bg-blue-600 px-5 text-[10px] font-black">Search</button></form><div className="mt-3 flex gap-2 overflow-x-auto"><span className="shrink-0 py-1 text-[9px] font-bold text-slate-300">Popular:</span>{config.popularTags.map((tag) => <button type="button" key={tag} onClick={() => onTag(tag)} className="shrink-0 rounded-full border border-white/20 bg-white/10 px-2.5 py-1 text-[9px] font-bold">{tag}</button>)}</div>{notice && <p className="mt-3 text-[9px] font-bold text-blue-200">{notice}</p>}</div></div></section>; }
