import { ChevronLeft, ChevronRight } from "lucide-react";
export default function CreatorSearchPagination({ page, pageCount, onPage }: { page: number; pageCount: number; onPage: (page: number) => void }) { if (pageCount <= 1) return null; const pages = Array.from({ length: pageCount }, (_, index) => index + 1); return <nav aria-label="Search result pages" className="mt-8 flex items-center justify-center gap-1"><button type="button" disabled={page === 1} onClick={() => onPage(page - 1)} className="grid h-8 w-8 place-items-center rounded-md border border-slate-200 bg-white disabled:opacity-40"><ChevronLeft className="h-4 w-4" /></button>{pages.map((value) => <button type="button" key={value} onClick={() => onPage(value)} aria-current={value === page ? "page" : undefined} className={`h-8 min-w-8 rounded-md px-2 text-[10px] font-black ${value === page ? "bg-blue-600 text-white" : "border border-slate-200 bg-white"}`}>{value}</button>)}<button type="button" disabled={page === pageCount} onClick={() => onPage(page + 1)} className="grid h-8 w-8 place-items-center rounded-md border border-slate-200 bg-white disabled:opacity-40"><ChevronRight className="h-4 w-4" /></button></nav>; }
