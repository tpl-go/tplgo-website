import { Grid2X2, List, X } from "lucide-react";

export default function CreatorSearchToolbar({ query, count, activeFilters, sort, view, onRemove, onClear, onSort, onView }: { query: string; count: number; activeFilters: [string, string][]; sort: string; view: "grid" | "list"; onRemove: (key: string) => void; onClear: () => void; onSort: (value: string) => void; onView: (view: "grid" | "list") => void }) {
  return <div><div className="flex flex-col gap-3 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-[10px] font-black uppercase tracking-[0.16em] text-blue-700">Results for</p><h1 className="mt-1 text-2xl font-black tracking-tight">{query ? `“${query}”` : "All creative assets"}</h1><p className="mt-1 text-xs font-semibold text-slate-500">{count.toLocaleString("en-IN")} assets found</p></div><div className="flex items-center gap-2"><select aria-label="Sort results" value={sort} onChange={(event) => onSort(event.target.value)} className="h-9 rounded-md border border-slate-200 bg-white px-3 text-[10px] font-bold"><option value="popular">Most Popular</option><option value="newest">Newest</option><option value="trending">Trending</option><option value="most-downloaded">Most Downloaded</option><option value="top-rated">Top Rated</option><option value="price-low">Price Low</option><option value="price-high">Price High</option></select><div className="flex rounded-md border border-slate-200 bg-white p-1"><button type="button" aria-label="Grid view" onClick={() => onView("grid")} className={`grid h-7 w-7 place-items-center rounded ${view === "grid" ? "bg-slate-900 text-white" : "text-slate-500"}`}><Grid2X2 className="h-3.5 w-3.5" /></button><button type="button" aria-label="List view" onClick={() => onView("list")} className={`grid h-7 w-7 place-items-center rounded ${view === "list" ? "bg-slate-900 text-white" : "text-slate-500"}`}><List className="h-3.5 w-3.5" /></button></div></div></div>{activeFilters.length > 0 && <div className="mt-4 flex flex-wrap items-center gap-2">{activeFilters.map(([key, value]) => <button type="button" key={key} onClick={() => onRemove(key)} className="inline-flex items-center gap-1 rounded-full border border-blue-200 bg-blue-50 px-2.5 py-1 text-[9px] font-black text-blue-700">{key}: {value}<X className="h-3 w-3" /></button>)}<button type="button" onClick={onClear} className="text-[9px] font-black text-slate-500 underline">Clear all</button></div>}</div>;
}
