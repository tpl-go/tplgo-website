import { Search, ShieldCheck } from "lucide-react";
import { useState } from "react";

const popular = ["India", "Himalayas", "Beach", "Aerial", "City", "Wildlife", "Sunset"];
export default function CreatorSearchBar({ initialQuery, type, safe, onSubmit, onType, onSafe, onPopular }: { initialQuery: string; type: string; safe: boolean; onSubmit: (value: string) => void; onType: (value: string) => void; onSafe: (value: boolean) => void; onPopular: (value: string) => void }) {
  const [query, setQuery] = useState(initialQuery);
  return <section className="border-b border-slate-200 bg-white"><div className="mx-auto max-w-[1440px] px-4 py-6 sm:px-6 lg:px-10"><form onSubmit={(event) => { event.preventDefault(); onSubmit(query); }} className="flex flex-col overflow-hidden rounded-xl border border-slate-300 bg-white shadow-lg shadow-slate-950/5 sm:flex-row"><select aria-label="Asset type" value={type} onChange={(event) => onType(event.target.value)} className="h-12 border-b border-slate-200 bg-slate-50 px-4 text-xs font-black outline-none sm:w-40 sm:border-b-0 sm:border-r"><option value="">All Assets</option><option value="photo">Photos</option><option value="video">Videos</option><option value="reel">Reels</option><option value="drone">Drone</option><option value="template">Templates</option><option value="presets">Presets</option><option value="graphics">Graphics</option><option value="guides">Destination Guides</option></select><div className="relative flex-1"><Search className="absolute left-4 top-4 h-4 w-4 text-slate-400" /><input suppressHydrationWarning value={query} onChange={(event) => setQuery(event.target.value)} className="h-12 w-full px-11 text-sm font-semibold outline-none" placeholder="Search millions of creative assets" /></div><button type="submit" className="h-12 bg-blue-600 px-8 text-xs font-black text-white hover:bg-blue-700">Search</button></form><div className="mt-3 flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><div className="flex items-center gap-2 overflow-x-auto"><span className="shrink-0 text-[10px] font-bold text-slate-400">Popular:</span>{popular.map((item) => <button type="button" key={item} onClick={() => onPopular(item)} className="shrink-0 rounded-full bg-slate-100 px-3 py-1.5 text-[10px] font-bold text-slate-600 hover:bg-blue-50 hover:text-blue-700">{item}</button>)}</div><label className="flex shrink-0 items-center gap-2 text-[10px] font-bold text-slate-600"><ShieldCheck className="h-3.5 w-3.5 text-blue-600" />Safe Search<button type="button" role="switch" aria-checked={safe} onClick={() => onSafe(!safe)} className={`relative h-5 w-9 rounded-full ${safe ? "bg-blue-600" : "bg-slate-300"}`}><span className={`absolute top-0.5 h-4 w-4 rounded-full bg-white transition ${safe ? "left-[18px]" : "left-0.5"}`} /></button></label></div></div></section>;
}
