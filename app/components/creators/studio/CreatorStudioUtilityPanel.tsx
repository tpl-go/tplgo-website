import Link from "next/link";
import { ArrowRight, CheckCircle2, Lightbulb } from "lucide-react";
import { studioModeration, studioQuickLinks, studioTips } from "@/app/lib/creators/creatorStudioData";

export default function CreatorStudioUtilityPanel() {
  return <aside className="space-y-4"><section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><p className="text-xs font-bold uppercase tracking-wide text-blue-700">Content health</p><h2 className="mt-1 text-lg font-bold">Moderation status</h2><div className="mt-4 grid grid-cols-3 gap-2">{studioModeration.map((item) => <div key={item.label} className="rounded-xl bg-slate-50 p-3 text-center"><strong className={`block text-xl ${item.tone}`}>{item.value}</strong><span className="mt-1 block text-xs font-semibold text-slate-600">{item.label}</span></div>)}</div><Link href="/creator-studio/moderation" className="mt-4 inline-flex items-center gap-1 text-sm font-bold text-blue-700">View moderation <ArrowRight className="h-4 w-4" /></Link></section><section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><div className="flex items-center gap-2"><Lightbulb className="h-5 w-5 text-amber-600" /><h2 className="text-lg font-bold">Creator tips</h2></div><ul className="mt-4 space-y-3">{studioTips.map((tip) => <li key={tip} className="flex gap-2 text-sm font-medium text-slate-600"><CheckCircle2 className="mt-0.5 h-4 w-4 shrink-0 text-emerald-600" />{tip}</li>)}</ul></section><section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><h2 className="text-lg font-bold">Quick links</h2><div className="mt-3 grid gap-1">{studioQuickLinks.map((item) => <Link key={item.label} href={item.href} className="flex min-h-10 items-center justify-between rounded-lg px-3 text-sm font-semibold text-slate-700 hover:bg-slate-50"><span>{item.label}</span><ArrowRight className="h-4 w-4 text-slate-400" /></Link>)}</div></section></aside>;
}
