"use client";

import Image from "next/image";
import Link from "next/link";
import { ArrowRight, CalendarDays, Ellipsis, TrendingUp } from "lucide-react";
import { studioActivity, studioCalendar, studioEarnings, studioOverview, studioQuickActions, studioRecentAssets, studioTopAssets } from "@/app/lib/creators/creatorStudioData";

export default function CreatorStudioDashboard() {
  return <div className="space-y-6"><section className="flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-xs font-bold uppercase tracking-[0.15em] text-blue-700">Creator Studio dashboard</p><h1 className="mt-2">Welcome back, Aira Studio</h1><p className="mt-2 font-medium text-slate-600">Here’s a preview of how your portfolio is performing across the TPL Creator marketplace.</p></div><span className="inline-flex w-fit items-center gap-2 rounded-lg border border-slate-200 bg-white px-3 py-2 text-sm font-semibold text-slate-600"><CalendarDays className="h-4 w-4 text-blue-700" />Last updated just now</span></section><section className="grid gap-3 sm:grid-cols-2 xl:grid-cols-5">{studioOverview.map(({ label, value, change, icon: Icon, tone }) => <article key={label} className="rounded-2xl border border-slate-200 bg-white p-4 shadow-sm"><span className={`grid h-10 w-10 place-items-center rounded-xl ${tone}`}><Icon className="h-5 w-5" /></span><strong className="mt-4 block text-2xl font-extrabold">{value}</strong><h2 className="mt-1 text-sm font-bold">{label}</h2><p className="mt-1 text-xs font-semibold text-slate-600">{change}</p></article>)}</section><section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><div className="flex items-end justify-between gap-4"><div><p className="text-xs font-bold uppercase tracking-wide text-blue-700">Start something</p><h2 className="mt-1">Quick actions</h2></div></div><div className="mt-4 grid gap-3 sm:grid-cols-2 xl:grid-cols-5">{studioQuickActions.map(({ label, href, icon: Icon }) => <Link key={label} href={href} className="flex min-h-20 items-center gap-3 rounded-xl border border-slate-200 p-4 font-bold text-slate-800 transition hover:border-blue-300 hover:bg-blue-50"><span className="grid h-10 w-10 shrink-0 place-items-center rounded-xl bg-blue-50 text-blue-700"><Icon className="h-5 w-5" /></span>{label}</Link>)}</div></section><section><div className="flex items-end justify-between gap-4"><div><p className="text-xs font-bold uppercase tracking-wide text-blue-700">Latest portfolio work</p><h2 className="mt-1">Recent assets</h2></div><Link href="/creator-studio/assets" className="text-sm font-bold text-blue-700">View all</Link></div><div className="mt-4 grid gap-4 md:grid-cols-2 2xl:grid-cols-3">{studioRecentAssets.map((asset) => <article key={asset.slug} className="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div className="relative aspect-[16/9] overflow-hidden"><Image src={asset.image} alt={asset.title} fill sizes="(min-width:1280px) 25vw, 50vw" className="object-cover" /><span className="absolute left-3 top-3 rounded bg-[#071831]/90 px-2 py-1 text-xs font-bold uppercase text-white">{asset.type}</span><Link href="/creator-studio/assets" aria-label={`Preview actions for ${asset.title}`} className="absolute right-3 top-3 grid h-9 w-9 place-items-center rounded-full bg-white shadow"><Ellipsis className="h-4 w-4" /></Link></div><div className="p-4"><h3 className="font-bold">{asset.title}</h3><p className="mt-1 text-xs font-medium text-slate-600">Uploaded {asset.uploaded}</p><div className="mt-4 grid grid-cols-2 gap-3 border-t border-slate-100 pt-3"><div><span className="text-xs font-medium text-slate-500">Downloads</span><strong className="mt-1 block text-sm">{asset.downloads}</strong></div><div><span className="text-xs font-medium text-slate-500">Revenue preview</span><strong className="mt-1 block text-sm text-emerald-700">{asset.revenue}</strong></div></div></div></article>)}</div></section><div className="grid gap-6 2xl:grid-cols-[1.15fr_.85fr]"><Activity /><Earnings /></div><TopAssets /><Calendar /></div>;
}

function Activity() { return <section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><p className="text-xs font-bold uppercase tracking-wide text-blue-700">Newest first</p><h2 className="mt-1">Recent activity</h2><ol className="mt-5 space-y-1">{studioActivity.map(({ title, detail, icon: Icon }, index) => <li key={title} className="relative flex gap-3 pb-5 last:pb-0">{index < studioActivity.length - 1 && <span className="absolute bottom-0 left-5 top-10 w-px bg-slate-200" />}<span className="relative grid h-10 w-10 shrink-0 place-items-center rounded-full bg-blue-50 text-blue-700"><Icon className="h-4 w-4" /></span><div className="pt-1"><p className="text-sm font-semibold">{title}</p><p className="mt-1 text-xs font-medium text-slate-600">{detail}</p></div></li>)}</ol></section>; }
function Earnings() { return <section className="rounded-2xl bg-[#071831] p-5 text-white shadow-xl"><p className="text-xs font-bold uppercase tracking-wide text-blue-200">Static preview</p><div className="mt-1 flex items-center justify-between"><h2 className="text-white">Earnings overview</h2><span className="text-2xl font-extrabold">₹1.84L</span></div><div className="mt-6 space-y-4">{studioEarnings.map((item) => <div key={item.label}><div className="flex justify-between gap-3 text-sm font-semibold text-slate-100"><span>{item.label}</span><span>{item.value}</span></div><div className="mt-2 h-2 overflow-hidden rounded-full bg-white/15"><div className="h-full rounded-full bg-blue-400" style={{ width: `${item.percent}%` }} /></div></div>)}</div><Link href="/creator-studio/revenue" className="mt-6 inline-flex items-center gap-1 text-sm font-bold text-blue-200">Revenue overview <ArrowRight className="h-4 w-4" /></Link></section>; }
function TopAssets() { return <section className="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div className="p-5"><p className="text-xs font-bold uppercase tracking-wide text-blue-700">Performance preview</p><h2 className="mt-1">Top performing assets</h2></div><div className="hidden overflow-x-auto md:block"><table className="w-full min-w-[680px] border-collapse text-left"><thead><tr className="bg-slate-50">{["Asset", "Downloads", "Revenue", "Conversion", "Trend"].map((item) => <th key={item} className="px-5 py-3 text-xs font-bold text-slate-600">{item}</th>)}</tr></thead><tbody>{studioTopAssets.map((item) => <tr key={item.asset}><td className="border-t border-slate-200 px-5 py-4 text-sm font-bold">{item.asset}</td><td className="border-t border-slate-200 px-5 py-4 text-sm font-semibold">{item.downloads}</td><td className="border-t border-slate-200 px-5 py-4 text-sm font-semibold">{item.revenue}</td><td className="border-t border-slate-200 px-5 py-4 text-sm font-semibold">{item.conversion}</td><td className="border-t border-slate-200 px-5 py-4 text-sm font-bold text-emerald-700">{item.trend}</td></tr>)}</tbody></table></div><div className="grid gap-3 p-3 md:hidden">{studioTopAssets.map((item) => <article key={item.asset} className="rounded-xl border border-slate-200 p-4"><h3 className="font-bold">{item.asset}</h3><div className="mt-3 grid grid-cols-2 gap-3 text-xs"><p><span className="text-slate-500">Downloads</span><strong className="mt-1 block">{item.downloads}</strong></p><p><span className="text-slate-500">Revenue</span><strong className="mt-1 block">{item.revenue}</strong></p><p><span className="text-slate-500">Conversion</span><strong className="mt-1 block">{item.conversion}</strong></p><p><span className="text-slate-500">Trend</span><strong className="mt-1 block text-emerald-700">{item.trend}</strong></p></div></article>)}</div></section>; }
function Calendar() { return <section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><div className="flex items-center gap-2"><CalendarDays className="h-5 w-5 text-blue-700" /><h2>Content calendar</h2></div><div className="mt-4 grid gap-3 md:grid-cols-3">{studioCalendar.map((item) => <article key={item.title} className="rounded-xl border border-slate-200 p-4"><span className="text-xs font-bold text-blue-700">{item.date}</span><h3 className="mt-2 font-bold">{item.title}</h3><p className="mt-1 text-xs font-medium text-slate-600">{item.type}</p></article>)}</div><p className="mt-4 inline-flex items-center gap-2 text-xs font-semibold text-slate-600"><TrendingUp className="h-4 w-4 text-emerald-600" />Calendar events are static configuration previews.</p></section>; }
