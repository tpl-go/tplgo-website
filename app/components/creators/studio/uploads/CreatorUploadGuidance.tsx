import { AlertTriangle, CheckCircle2, Lightbulb, ListChecks, X } from "lucide-react";
import { publishingTips, uploadGuidelines, uploadSteps } from "@/app/lib/creators/creatorUploadWizardData";

export default function CreatorUploadGuidance({ current, warnings, open, onClose }: { current: number; warnings: string[]; open: boolean; onClose: () => void }) {
  const content = <div className="space-y-4"><div className="flex items-start justify-between gap-3 lg:hidden"><div><p className="text-xs font-bold uppercase tracking-wide text-blue-700">Publishing support</p><h2 className="mt-1">Upload guidance</h2></div><button type="button" onClick={onClose} className="grid h-10 w-10 place-items-center rounded-lg border border-slate-200" aria-label="Close upload guidance"><X className="h-5 w-5" /></button></div><section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><div className="flex items-center gap-2"><ListChecks className="h-5 w-5 text-blue-700" /><h2 className="text-lg">Upload steps</h2></div><div className="mt-4 h-2 overflow-hidden rounded-full bg-slate-200"><div className="h-full rounded-full bg-blue-600" style={{ width: `${((current + 1) / uploadSteps.length) * 100}%` }} /></div><p className="mt-2 text-xs font-semibold text-slate-600">Step {current + 1} of {uploadSteps.length} · {uploadSteps[current].label}</p></section><section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><h2 className="text-lg">Guidelines</h2><ul className="mt-3 space-y-3">{uploadGuidelines.map((item) => <li key={item} className="flex gap-2 text-sm font-medium text-slate-600"><CheckCircle2 className="mt-0.5 h-4 w-4 shrink-0 text-emerald-600" />{item}</li>)}</ul></section><section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><div className="flex items-center gap-2"><Lightbulb className="h-5 w-5 text-amber-600" /><h2 className="text-lg">Publishing tips</h2></div><ul className="mt-3 space-y-2">{publishingTips.map((item) => <li key={item} className="text-sm font-medium text-slate-600">• {item}</li>)}</ul></section>{warnings.length > 0 && <section className="rounded-2xl border border-amber-200 bg-amber-50 p-5"><div className="flex items-center gap-2"><AlertTriangle className="h-5 w-5 text-amber-700" /><h2 className="text-lg text-amber-950">Warnings</h2></div><ul className="mt-3 space-y-2">{warnings.map((item) => <li key={item} className="text-sm font-semibold text-amber-900">• {item}</li>)}</ul></section>}</div>;
  return <><aside className="hidden lg:block">{content}</aside>{open && <div className="fixed inset-0 z-50 lg:hidden"><button type="button" onClick={onClose} className="absolute inset-0 bg-slate-950/60" aria-label="Close upload guidance backdrop" /><aside className="absolute inset-y-0 right-0 w-[min(90vw,360px)] overflow-y-auto bg-[#f4f7fb] p-4 shadow-2xl">{content}</aside></div>}</>;
}
