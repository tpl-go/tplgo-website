import type { InputHTMLAttributes, SelectHTMLAttributes, TextareaHTMLAttributes } from "react";

const base = "mt-1.5 min-h-11 w-full rounded-lg border border-slate-300 bg-white px-3 py-2 text-sm font-medium text-slate-950 outline-none placeholder:text-slate-500 focus:border-blue-500 focus:ring-2 focus:ring-blue-100";
export function UploadField({ label, error, help, ...props }: InputHTMLAttributes<HTMLInputElement> & { label: string; error?: string; help?: string }) { return <label className="block text-sm font-semibold text-slate-800">{label}<input {...props} className={`${base} ${error ? "border-rose-500" : ""}`} />{error ? <span className="mt-1 block text-xs font-semibold text-rose-700">{error}</span> : help ? <span className="mt-1 block text-xs font-medium text-slate-600">{help}</span> : null}</label>; }
export function UploadTextarea({ label, error, help, ...props }: TextareaHTMLAttributes<HTMLTextAreaElement> & { label: string; error?: string; help?: string }) { return <label className="block text-sm font-semibold text-slate-800">{label}<textarea {...props} className={`${base} min-h-28 resize-y ${error ? "border-rose-500" : ""}`} />{error ? <span className="mt-1 block text-xs font-semibold text-rose-700">{error}</span> : help ? <span className="mt-1 block text-xs font-medium text-slate-600">{help}</span> : null}</label>; }
export function UploadSelect({ label, error, children, ...props }: SelectHTMLAttributes<HTMLSelectElement> & { label: string; error?: string }) { return <label className="block text-sm font-semibold text-slate-800">{label}<select {...props} className={`${base} ${error ? "border-rose-500" : ""}`}>{children}</select>{error && <span className="mt-1 block text-xs font-semibold text-rose-700">{error}</span>}</label>; }
export function UploadStepIntro({ step, title, copy }: { step: string; title: string; copy: string }) { return <div><p className="text-xs font-bold uppercase tracking-[0.15em] text-blue-700">{step}</p><h1 className="mt-2">{title}</h1><p className="mt-3 max-w-3xl font-medium text-slate-600">{copy}</p></div>; }
