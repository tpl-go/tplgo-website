import Image from "next/image";
import Link from "next/link";
import { ArrowRight, TrendingUp } from "lucide-react";
import { analyticsTopAssets, engagementFunnel } from "@/app/lib/creators/creatorAnalyticsData";

export function CreatorTopAssetsTable() { return <section className="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div className="flex items-end justify-between gap-4 p-5"><div><p className="text-xs font-bold uppercase tracking-wide text-blue-700">Performance preview</p><h2 className="mt-1">Top Performing Assets</h2></div><Link href="/creator-studio/assets" className="inline-flex items-center gap-1 text-sm font-bold text-blue-700">View All <ArrowRight className="h-4 w-4" /></Link></div><div className="hidden md:block"><table className="w-full border-collapse text-left"><thead><tr className="bg-slate-50">{["Asset", "Downloads", "Revenue", "Conversion", "Trend"].map((item) => <th key={item} className="px-5 py-3 text-xs font-bold text-slate-600">{item}</th>)}</tr></thead><tbody>{analyticsTopAssets.map((asset) => <tr key={asset.title} className="border-t border-slate-200"><td className="px-5 py-3"><div className="flex items-center gap-3"><div className="relative h-12 w-20 overflow-hidden rounded-lg"><Image src={asset.image} alt="" fill sizes="80px" className="object-cover" /></div><span className="text-sm font-bold">{asset.title}</span></div></td><td className="px-5 py-3 text-sm font-semibold">{asset.downloads}</td><td className="px-5 py-3 text-sm font-semibold">{asset.revenue}</td><td className="px-5 py-3 text-sm font-semibold">{asset.conversion}</td><td className="px-5 py-3 text-sm font-bold text-emerald-700">{asset.trend}</td></tr>)}</tbody></table></div><div className="grid gap-3 p-3 md:hidden">{analyticsTopAssets.map((asset) => <article key={asset.title} className="rounded-xl border border-slate-200 p-4"><div className="flex gap-3"><div className="relative h-16 w-24 shrink-0 overflow-hidden rounded-lg"><Image src={asset.image} alt="" fill sizes="96px" className="object-cover" /></div><div><h3 className="text-sm font-bold">{asset.title}</h3><p className="mt-2 inline-flex items-center gap-1 text-xs font-bold text-emerald-700"><TrendingUp className="h-3.5 w-3.5" />{asset.trend}</p></div></div><div className="mt-3 grid grid-cols-3 gap-2 border-t border-slate-100 pt-3">{[["Downloads", asset.downloads], ["Revenue", asset.revenue], ["Conversion", asset.conversion]].map(([label, value]) => <p key={label}><span className="block text-xs text-slate-500">{label}</span><strong className="mt-1 block text-xs">{value}</strong></p>)}</div></article>)}</div></section>; }

export function CreatorEngagementFunnel() { const max = engagementFunnel[0].value; return <section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><h2 className="text-lg">Engagement Funnel</h2><p className="mt-1 text-xs font-medium text-slate-600">From marketplace impression to download.</p><div className="mt-5 space-y-3">{engagementFunnel.map((item, index) => <div key={item.label} className="mx-auto rounded-lg bg-gradient-to-r from-blue-600 to-violet-600 px-4 py-3 text-white" style={{ width: `${Math.max(48, (item.value / max) * 100)}%` }}><div className="flex justify-between gap-3 text-xs font-bold"><span>{index + 1}. {item.label}</span><span>{item.display}</span></div></div>)}</div></section>; }
