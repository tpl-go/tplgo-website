import { ArrowDownRight, ArrowUpRight } from "lucide-react";
import { analyticsKPIs } from "@/app/lib/creators/creatorAnalyticsData";

export default function CreatorAnalyticsKPIs() { return <section className="grid gap-3 sm:grid-cols-2 2xl:grid-cols-3">{analyticsKPIs.map((kpi) => <article key={kpi.key} className="rounded-2xl border border-slate-200 bg-white p-4 shadow-sm"><div className="flex items-start justify-between gap-3"><div><p className="text-sm font-semibold text-slate-600">{kpi.label}</p><strong className="mt-2 block text-2xl font-extrabold">{kpi.value}</strong></div><Sparkline points={kpi.points} /></div><p className={`mt-3 inline-flex items-center gap-1 text-xs font-bold ${kpi.growth >= 0 ? "text-emerald-700" : "text-rose-700"}`}>{kpi.growth >= 0 ? <ArrowUpRight className="h-3.5 w-3.5" /> : <ArrowDownRight className="h-3.5 w-3.5" />}{Math.abs(kpi.growth)}% <span className="font-medium text-slate-500">{kpi.comparison}</span></p></article>)}</section>; }

function Sparkline({ points }: { points: number[] }) { const max = Math.max(...points); const min = Math.min(...points); const coords = points.map((point, index) => `${(index / (points.length - 1)) * 94 + 3},${42 - ((point - min) / Math.max(1, max - min)) * 32}`).join(" "); return <svg aria-label="Preview trend sparkline" viewBox="0 0 100 48" className="h-12 w-24"><polyline points={coords} fill="none" stroke="#2563eb" strokeWidth="3" strokeLinecap="round" strokeLinejoin="round" /><path d="M3 43 H97" stroke="#e2e8f0" strokeWidth="1" /></svg>; }
