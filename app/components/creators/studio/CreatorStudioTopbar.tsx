import Link from "next/link";
import { Bell, ChevronDown, Menu, MessageSquare, Plus, Search } from "lucide-react";

export default function CreatorStudioTopbar({ query, date, onQuery, onDate, onMenu, onPreview }: { query: string; date: string; onQuery: (value: string) => void; onDate: (value: string) => void; onMenu: () => void; onPreview: (message: string) => void }) {
  return <header className="sticky top-0 z-30 border-b border-slate-200 bg-white/95 backdrop-blur"><div className="flex min-h-16 items-center gap-2 px-4 sm:px-6"><button type="button" onClick={onMenu} className="grid h-11 w-11 shrink-0 place-items-center rounded-xl border border-slate-200 lg:hidden" aria-label="Open Studio navigation"><Menu className="h-5 w-5" /></button><div className="relative hidden max-w-md flex-1 md:block"><Search className="absolute left-3 top-1/2 h-4 w-4 -translate-y-1/2 text-slate-500" /><input value={query} onChange={(event) => onQuery(event.target.value)} placeholder="Search your Studio..." className="h-11 w-full rounded-xl border border-slate-200 bg-slate-50 pl-10 pr-3 text-sm font-medium outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-100" /></div><div className="ml-auto flex items-center gap-2"><select aria-label="Dashboard date range" value={date} onChange={(event) => onDate(event.target.value)} className="hidden h-10 rounded-lg border border-slate-200 bg-white px-3 text-sm font-semibold text-slate-700 sm:block"><option>Last 30 days</option><option>Last 7 days</option><option>This year</option><option>All time</option></select><Link href="/creator-studio/uploads" className="inline-flex min-h-10 items-center gap-2 rounded-lg bg-blue-600 px-3 py-2 text-sm font-bold text-white hover:bg-blue-500"><Plus className="h-4 w-4" /><span className="hidden sm:inline">Upload</span></Link><button type="button" onClick={() => onPreview("Notification center preview opened.")} className="relative grid h-10 w-10 place-items-center rounded-lg border border-slate-200" aria-label="Notifications"><Bell className="h-4 w-4" /><span className="absolute right-2 top-2 h-2 w-2 rounded-full bg-rose-500" /></button><button type="button" onClick={() => onPreview("Messages preview opened — no conversation service is enabled.")} className="hidden h-10 w-10 place-items-center rounded-lg border border-slate-200 sm:grid" aria-label="Messages"><MessageSquare className="h-4 w-4" /></button><button type="button" onClick={() => onPreview("Workspace menu preview opened.")} className="flex h-10 items-center gap-2 rounded-lg border border-slate-200 px-2" aria-label="Workspace menu"><span className="grid h-7 w-7 place-items-center rounded-full bg-violet-600 text-xs font-bold text-white">AS</span><ChevronDown className="h-3.5 w-3.5 text-slate-500" /></button></div></div><div className="border-t border-slate-100 px-4 py-2 md:hidden"><div className="relative"><Search className="absolute left-3 top-1/2 h-4 w-4 -translate-y-1/2 text-slate-500" /><input value={query} onChange={(event) => onQuery(event.target.value)} placeholder="Search your Studio..." className="h-10 w-full rounded-lg border border-slate-200 bg-slate-50 pl-10 pr-3 text-sm font-medium outline-none" /></div></div></header>;
}
