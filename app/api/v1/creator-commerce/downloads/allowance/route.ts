import { commerceError,commerceOk,commerceUser } from "@/app/lib/creators/creatorCommerceApi";import { creatorCommerceFlags } from "@/app/lib/creators/creatorCommerceFlags";import { creatorTestingCommerceRepository } from "@/app/lib/creators/creatorTestingCommerceRepository";
export async function GET(request:Request){if(!creatorCommerceFlags.testDownloadsEnabled())return commerceError("DELIVERY_DISABLED","Testing downloads are disabled.",503);const buyer=commerceUser(request);if(!buyer)return commerceError("AUTH_REQUIRED","Shared TPL login is required.",401);return commerceOk(creatorTestingCommerceRepository.list(buyer).map(item=>({entitlementId:item.entitlement.entitlementId,status:item.entitlement.status,downloadsUsed:item.entitlement.downloadsUsed,downloadsRemaining:item.entitlement.downloadsRemaining,resetDate:item.entitlement.planAllowance?.resetDate??null,lastDownloadAt:item.timeline.filter(value=>value.status==="testing_download_complete").at(-1)?.at??null})));}
